# STATUS: UNBLOCKED 2026-09-26. The ERR_REQUIRE_ESM block below is resolved by
# giving decode-uri-component 0.5.0 (the fixed release) a CommonJS entry in the
# build, the same way brace-expansion 5.x is shimmed: the installed package IS
# 0.5.0, only its module format changes, and melange.yaml asserts every copy and
# that query-string parses through it. See melange.yaml for the step.
#
# STATUS: BLOCKED 2026-09-19, and the reason has CHANGED. The zlib wall it
# used to cite is gone (Wolfi shipped the CVE-2026-85091 fix as
# 1.3.2.1_rc20260601-r0). Unblocking on that basis produced run 35466118460,
# where BOTH arches passed the 0-CVE gate and then FAILED the boot test:
#
#   Error [ERR_REQUIRE_ESM]: require() of ES Module
#   /app/node_modules/decode-uri-component/index.js from
#   /app/node_modules/query-string/index.js not supported.
#
# The cause is a CVE pin in melange.yaml: decode-uri-component is pinned to
# 0.5.0 for CVE-2026-45822. There is no version pair that is both loadable
# from CommonJS and free of that CVE:
#
#   decode-uri-component  0.3.0 is the last CJS line; 0.4.0+ are "type":"module".
#                         (This line used to say 0.3.0+ were ESM. Re-measured
#                         2026-09-20 against the npm registry: 0.3.0 is
#                         commonjs, 0.4.0 is the first module. It changes
#                         nothing, because OSV still puts the fixed range at
#                         [0, 0.5.0), so 0.3.0 carries the CVE too.)
#                         CVE-2026-45822 affects everything BELOW 0.5.0.
#   query-string          7.1.3 is CJS and requires decode-uri-component.
#                         8.0.0+ are ESM-only, so soketi's compiled CJS
#                         dist/server.js cannot require query-string either.
#
# So the pin is correct for the scanner and fatal at runtime, and the block is
# what hid it: this app has not been rebuilt since 2026-08-26, and a blocked
# app never reaches CI's boot test. Do NOT "fix" this by reverting to 0.2.2;
# that reintroduces the CVE.
#
# The way out is one of: soketi moving to ESM upstream or dropping
# query-string, or patching the vulnerable decode path in 0.2.2 in-tree the
# way melange.yaml already shims brace-expansion. The second is real work and
# a deliberate call, not a quick swap.
#
# Re-checked 2026-09-20 against upstream 1.0.18: nothing moves. soketi 1.6.1 is
# still its newest release, so the query-string dependency that forces CJS is
# unchanged, and VERSIONS stays at 1.0.17 rather than declaring a version that
# cannot be gated. An in-tree patch of 0.2.2 remains the only path under our
# control and remains a deliberate call: patching the source does not change
# the version string Trivy reads, so it only works paired with evidence strong
# enough for a VEX. Do not reach for that to make a number go down.
#
# Still true and still unpublished: the otel float in melange.yaml closes
# CVE-2026-81870, and the published 1.0.17 (2026-08-26) predates it.
# STATUS: UNBLOCKED, re-gated 2026-09-14 -- nightly scan (4 HIGH/10 MEDIUM/1 LOW/1 UNKNOWN
#   against the published sha256:f128e512... digest, tag 1.0.17) was entirely STALE-IN-SCAN
#   or fixed-in-place today, not a live hole:
#     * grpc CVE-2026-84304/-84445/-84303 (cloudflared) + x/crypto CVE-2026-56855/-78662
#       (cloudflared) -- ALREADY FIXED IN RECIPE as of commit b69c98e (2026-09-06, grpc
#       v1.82.1->v1.83.2, x/crypto v0.55.0->v0.56.0); the scanned digest simply predates
#       that commit and has not been rebuilt+republished since. A plain rebuild clears all 5.
#     * The cloudflared build step used a BARE `go get M@V` pin (exact version, silently
#       downgrades once cloudflared's own go.mod needs more) instead of this repo's
#       FLOOR + below_floor idiom -- converted today (melange.yaml) to the proper floor
#       pattern so this class of drift can't recur silently. Verified with a real
#       `go build ./cmd/cloudflared` locally (go1.27, cloudflared 2026.7.1 checkout):
#       resolves x/net v0.58.0, x/sys v0.47.0, grpc v1.83.2, x/crypto v0.56.0, x/text
#       v0.41.0, gorilla/websocket v1.5.3 and compiles clean.
#     * js-yaml CVE-2026-84375 (installed 4.3.1, fix 4.3.2) and mysql2 GHSA-3f6p-5ww8-9rcr
#       + GHSA-rgwj-5xj2-c3m3 (installed 3.9.8, fix 3.22.0/3.23.1) -- REAL, not yet in the
#       npm overrides pins. FIXED: js-yaml bumped 4.3.1->4.3.2, mysql2 bumped 3.9.8->3.24.4
#       (newest on the 3.x line, clears both advisories) in the soketi override block.
#     * decode-uri-component CVE-2026-45822 (installed 0.2.2, fix 0.5.0) -- REAL, had no
#       override at all. FIXED: added "decode-uri-component": "0.5.0" to the pins object
#       (npm's registry lists no version between 0.4.1 and the fixed 0.5.0).
#     * glibc-2.44 / glibc-2.44-locale-posix / ld-linux-2.44 / libcrypt1-2.44 CVE-2026-18374
#       (installed 2.44-r1, fix 2.44-r6) -- WOLFI APK LAG on the published digest only.
#       Wolfi's newest glibc-2.44 apk (r6, verified via APKINDEX today) already carries the
#       fix and nothing pins an older release in apko.yaml/melange.yaml -- a plain rebuild
#       resolves the newest apk and clears all 4 rows. NO EDIT NEEDED.
#   REMAINS BLOCKED (2, unchanged, re-verified today):
#     * zlib CVE-2026-85091 (MEDIUM) needs zlib 1.3.3-r0. Wolfi's newest is 1.3.2-r6 on both
#       arches (checked APKINDEX today) -- the known repo-wide wall, not fixable by any
#       recipe edit here.
#     * aws-sdk (JS v2) GHSA-j965-2qgj-vjmq (LOW): upstream ships no fixed v2 release
#       (advisory says migrate to v3); `fixed: null` in the security feed. Soketi's cloud
#       driver imports aws-sdk statically (see the header comment) so it can't be pruned
#       without breaking the load path. No override target exists -- BLOCKED, no fix upstream.
#     * GO-2026-5932 (UNKNOWN): golang.org/x/crypto/openpgp is flagged "unmaintained, unsafe
#       by design" -- a design advisory with `fixed: null`, not a version bug; no x/crypto
#       release clears it (openpgp is deprecated in place, upstream tells callers to migrate
#       off it entirely). cloudflared's own dependency, not ours to rewrite. BLOCKED.
#
# STATUS: UNBLOCKED 2026-07-28 -- npm brace-expansion CVE-2026-14257 (GHSA-mh99-v99m-4gvg,
#   HIGH: unbounded expansion length -> OOM process crash) is FIXED here, not waived.
#
#   Dependency path (soketi prod tree, the only one -- the terminal tree's lock carries no
#   brace-expansion at all: @xterm/*, cookie, dotenv, node-addon-api, node-pty, ws):
#     bullmq 1.91.1 -> glob ^8.0.3 -> minimatch (5.1.8, glob-scoped override) -> brace-expansion
#   pm2's old path (pm2 -> yamljs -> glob 7 -> minimatch 3) is already gone: the pinned
#   pm2 7.0.3 dropped yamljs for js-yaml.
#
#   THE FIX (melange.yaml, soketi step): the global override now pins brace-expansion 5.0.8
#   -- the ONLY patched release, since the advisory's single affected range `<= 5.0.7` spans
#   every line and the maintenance heads (1.1.16, 2.1.2) are themselves affected -- and the
#   post-install step appends one line to each installed copy's dist/commonjs/index.js:
#     module.exports = Object.assign(expand, exports, { default: expand });
#   5.0.8 rewrote that entry to a NAMED-only export (`exports.expand`, `__esModule: true`, no
#   callable default) while minimatch 3.1.5 and 5.1.8 both do `require("brace-expansion")(pat)`,
#   so without the re-export soketi dies at LOAD time with "expand is not a function". `function
#   expand` is a top-level declaration in that file, so the identifier resolves and the patched
#   implementation -- the EXPANSION_MAX_LENGTH accumulator bound that fixes the CVE -- is what
#   runs. NOT a gate bypass: no .trivyignore, no --skip, no VEX; the installed package genuinely
#   IS the fixed version. Same shim already shipping green in apps/{n8n,n8n-runners,ghost,renovate}.
#   Copies are enumerated with `find`, never fs.readdirSync({recursive:true}) -- the readdir
#   walker matched only the hoisted top-level copy elsewhere and let nested ones ship vulnerable.
#   The build asserts (a) no copy escaped the pin and (b) every copy require()s to a callable
#   that expands `a{b,c}` -> ab,ac, plus every minimatch copy still braceExpand()s, so a stale
#   copy or a future 5.x that moves the CJS entry fails the build loudly.
#
#   THE nodejs-18 QUESTION -- RESOLVED, was the last real doubt. 5.0.8 (and its balanced-match
#   ^4 dep) declare `engines: "20 || >=22"`, but that is a SUPPORT statement, not a syntax or
#   API floor: both compiled CJS entries are plain ES2018 with zero Node-20-only API (grepped
#   the tarballs for structuredClone / findLast / toSorted / Object.groupBy / node: specifiers /
#   getBuiltinModule / private fields -- none present). Proven by execution on real Node 18.20.8:
#   brace-expansion 5.0.8 + balanced-match 4.0.4 load, EXPANSION_MAX_LENGTH is present, and with
#   the shim appended minimatch 3.1.5 AND 5.1.8 braceExpand `a{b,c}` -> ab,ac while glob 8 globs.
#   npm only warns (EBADENGINE); it does not refuse. So the image keeps its nodejs-18 pin (still
#   forced by soketi 1.6.1's uWebSockets.js 20.10.0, ABI <= 108) and gets the fix anyway. The
#   old "parent bump" escape routes stay dead and stay unnecessary: minimatch 9.0.9 REVERTED to
#   brace-expansion ^2.0.2, and minimatch 10 is ESM-first with a CJS shape glob 8 can't consume.
#   Gate 2026-07-28 (fresh Trivy DB): 0 fixable CVEs on x86_64 + aarch64, and test.sh green --
#   which is the load-time proof that matters here: it boots the container read-only and requires
#   soketi to answer HTTP 200 "OK" on :6001 / and :6001/ready, so a broken minimatch would fail it.
#
# History -- BLOCKED 2026-07-26 -> 2026-07-28 on the above (no fix existed below 5.0.8 in any
#   line, and 5.0.8's NAMED-only CJS export broke minimatch at load); resolved by the shim.
#
# History -- UNBLOCKED 2026-07-10: the two residual CVEs (CVE-2026-41178 OTel-Go +
#   CVE-2025-69725 go-chi) came from Wolfi's LAGGING `cloudflared` apk (newest published =
#   2026.5.2-r2; the fixed 2026.6.1-r2 never landed). cloudflared is now BUILT FROM SOURCE
#   in melange.yaml at its newest release tag (2026.7.1, go-1.26) whose module graph is
#   already advisory-clean (otel 1.43.0, go-chi 5.3.1 -- zero `go get` bumps needed),
#   installed at /usr/bin/cloudflared, and the Wolfi apk was dropped from apko.yaml.
#   Node/soketi tree unchanged (0-CVE at the time via the npm override pins). Gate
#   re-verified 2026-07-10 (fresh Trivy DB): 0 fixable CVEs + test.sh green.
#
# coolify-realtime: from-source via melange; version pinned in melange.yaml. No render placeholders.
BLOCKED=0
FROM_SOURCE=1
VERSIONS=(1.0.17)
render() { echo "s/__VER__/$1/g"; }
