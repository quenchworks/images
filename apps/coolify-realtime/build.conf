# UNBLOCKED 2026-07-10: the two residual CVEs (CVE-2026-41178 OTel-Go + CVE-2025-69725
#   go-chi) came from Wolfi's LAGGING `cloudflared` apk (newest published = 2026.5.2-r2;
#   the fixed 2026.6.1-r2 never landed). cloudflared is now BUILT FROM SOURCE in
#   melange.yaml at its newest release tag (2026.7.1, go-1.26) whose module graph is
#   already advisory-clean (otel 1.43.0, go-chi 5.3.1 -- zero `go get` bumps needed),
#   installed at /usr/bin/cloudflared, and the Wolfi apk was dropped from apko.yaml.
#   Node/soketi tree unchanged (still 0-CVE via the npm override pins). Gate re-verified
#   2026-07-10 (fresh Trivy DB): 0 fixable CVEs + test.sh green.
#
# coolify-realtime: from-source via melange; version pinned in melange.yaml. No render placeholders.
FROM_SOURCE=1
VERSIONS=(1.0.16)
render() { echo "s/__VER__/$1/g"; }
