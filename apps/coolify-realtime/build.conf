# STATUS: BLOCKED 2026-07-26 (do not publish) -- npm brace-expansion CVE-2026-14257
#   (GHSA-mh99-v99m-4gvg, HIGH: unbounded expansion length -> OOM process crash) in
#   /app/node_modules. No shippable fix exists for this image today:
#
#   Dependency path (soketi prod tree): bullmq 1.91.1 -> glob ^8.0.3 -> minimatch
#   (pinned 5.1.8 by the scoped glob override) -> brace-expansion (pinned 1.1.16 by the
#   global override in melange.yaml). pm2's old path (pm2 -> yamljs -> glob 7 ->
#   minimatch 3) is already gone: the pinned pm2 7.0.3 dropped yamljs for js-yaml.
#
#   1. There is NO 1.x/2.x/3.x/4.x fix. The advisory's affected range is `<= 5.0.7`
#      across EVERY line and the only patched version is 5.0.8; the `maintenance-v1`
#      dist-tag (1.1.16) and `maintenance-v2` (2.1.2) are themselves affected. The
#      1.1.16 pin already in melange.yaml is still the maximum the 1.x line offers (it
#      does clear CVE-2026-13149 / -33750 / CVE-2025-5889), so it stays.
#   2. A blunt `overrides: { "brace-expansion": "5.0.8" }` WOULD BREAK the tree, verified
#      from the published tarballs, not assumed: 5.0.8's CJS entry (dist/commonjs/
#      index.js) exports a NAMED binding (`exports.expand = expand`, `__esModule: true`)
#      with no callable default, while minimatch 3.1.5 (minimatch.js:10,271) and 5.1.8
#      (minimatch.js:119) both do `var expand = require('brace-expansion')` and then call
#      `expand(pattern)` -> TypeError: expand is not a function, at soketi load time.
#   3. The parent bump is blocked by this image's Node 18 floor. Only minimatch >= 9.0.6
#      /10.1.3 consume brace-expansion ^5, and: 9.0.9 (newest 9.x) REVERTED to
#      brace-expansion ^2.0.2 (still affected), and minimatch 10.2.5 pulls
#      brace-expansion ^5.0.5 -> 5.0.8, whose own `engines` is `20 || >=22`. This image
#      is hard-pinned to nodejs-18 for BOTH build and runtime because soketi 1.6.1's
#      uWebSockets.js 20.10.0 only bundles prebuilts up to the Node-18 ABI (108) -- see
#      the melange.yaml header. minimatch 10 is also ESM-first and changes the CJS export
#      shape its consumer here (glob 8, required by bullmq 1.91.1) depends on.
#
#   UN-BLOCK when ANY of these lands (re-check on the nightly sweep):
#     a. brace-expansion backports the EXPANSION_MAX_LENGTH bound to a maintenance line
#        (a 1.1.17 or 2.1.3) -> bump the existing global pin, one-line fix; or
#     b. soketi ships a release whose uWebSockets.js supports Node >= 20 -> lift the
#        nodejs-18 pin, then glob 11 / minimatch 10 / brace-expansion 5.0.8 all become
#        installable and supported; or
#     c. bullmq >= 5 is validated as a drop-in for soketi's compiled queue driver.
#        bullmq 5.x DROPPED glob from its dependencies entirely (verified against the
#        registry), which deletes this whole path instead of patching it -- but it is a
#        1.x -> 5.x major on a module soketi statically imports, and it collides with the
#        existing msgpackr 1.10.1 pin, so it needs a real build + test.sh run to justify.
#
# History -- UNBLOCKED 2026-07-10: the two residual CVEs (CVE-2026-41178 OTel-Go +
#   CVE-2025-69725 go-chi) came from Wolfi's LAGGING `cloudflared` apk (newest published =
#   2026.5.2-r2; the fixed 2026.6.1-r2 never landed). cloudflared is now BUILT FROM SOURCE
#   in melange.yaml at its newest release tag (2026.7.1, go-1.26) whose module graph is
#   already advisory-clean (otel 1.43.0, go-chi 5.3.1 -- zero `go get` bumps needed),
#   installed at /usr/bin/cloudflared, and the Wolfi apk was dropped from apko.yaml.
#   Node/soketi tree unchanged (0-CVE at the time via the npm override pins). Gate
#   re-verified 2026-07-10 (fresh Trivy DB): 0 fixable CVEs + test.sh green.
#
# coolify-realtime: from-source via melange; version pinned in melange.yaml. No render placeholders.
FROM_SOURCE=1
BLOCKED=1  # brace-expansion CVE-2026-14257: fix exists only in 5.0.8 (Node 20+, CJS API break) -- see STATUS above
VERSIONS=(1.0.16)
render() { echo "s/__VER__/$1/g"; }
