# re-verified 2026-07-09 (fresh Trivy DB): STILL BLOCKED. Isolated apko+trivy probe
#   of the Wolfi cloudflared apk (newest in live index = 2026.5.2-r2) still reports
#   CVE-2026-41178 (OTel-Go) + CVE-2025-69725 (go-chi), both fixed only in the
#   not-yet-published 2026.6.1-r2. Kept the WIP `npm=~10` pin (a build fix, unrelated
#   to the CVE block). cloudflared is a Wolfi apk, not built from source here, so
#   nothing in this recipe can clear it; dropping it would be a functional regression.
# re-gated 2026-07-08 (fresh Trivy DB, live Wolfi APKINDEX): STILL BLOCKED.
#   Node/soketi tree scans 0-CVE; the only 2 residual CVEs are still from the
#   Wolfi cloudflared apk (installed 2026.5.2-r2, the newest in the index):
#   CVE-2026-41178 (OpenTelemetry-Go) + CVE-2025-69725 (go-chi). Fix needs
#   cloudflared >= 2026.6.1-r2, not yet published by Wolfi. Pure distro lag.
# STATUS: BLOCKED (do not publish)
#   The node dependency tree is 0-CVE (soketi 1.6.1's 117 fixable node-pkg CVEs are all
#   pinned to fixed releases via npm overrides in melange.yaml; the terminal server + the
#   node-pty native build are clean). The ONLY residual fixable CVEs come from the Wolfi
#   `cloudflared` package, which the recipe ships CVE-tracked as a required runtime dep
#   (the `cloudflared access ssh` ProxyCommand for Cloudflare-tunneled targets) and which
#   cannot be dropped without a functional regression:
#     - cloudflared            CVE-2026-41178  MEDIUM  (OpenTelemetry-Go)  fixed in cloudflared 2026.6.1-r2
#     - github.com/go-chi/chi/v5 CVE-2025-69725 MEDIUM (open redirect)    fixed in go-chi 5.2.4 (rebuilt cloudflared)
#   Wolfi's newest published cloudflared is 2026.5.2-r2 (apko already installs it); the
#   fixed 2026.6.1-r2 is not yet in the Wolfi index. This is a transient upstream-distro
#   lag: once Wolfi publishes cloudflared >= 2026.6.1-r2, a plain rebuild passes 0-CVE
#   with NO further code change. Remove this header when the gate is clean.
#
# coolify-realtime: from-source via melange; version pinned in melange.yaml. No render placeholders.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(1.0.16)
render() { echo "s/__VER__/$1/g"; }