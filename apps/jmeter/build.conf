# STATUS: BLOCKED (do not publish)
# Re-triaged 2026-07-08: 5.6.3 is STILL the newest upstream release (no 5.6.4/5.7),
# and the gate still fails on the same hard blockers below (shaded netty in
# neo4j-java-driver-4.4.13.jar; major-bump tika-core/tika-parsers 1.28.5 CRITICAL;
# major-bump dnsjava 2.1.9 HIGH). Several standalone same-major swaps also flag now
# (batik 1.16, commons-lang3 3.14.0, jackson 2.16.1, json-path 2.8.0, json-smart
# 2.5.0, log4j 2.22.1, xstream 1.4.20) but are moot while the shaded/major blockers
# below stay red.
# The 0-CVE Trivy gate CANNOT pass for Apache JMeter 5.6.3 (newest upstream
# release; no newer 5.6.x/5.7 line exists) and no minimal same-major standalone
# swap clears it. Blocking CVEs that are NOT minimally fixable:
#   - SHADED (uber-jar): io.netty netty-codec / netty-common / netty-handler are
#     bundled INSIDE neo4j-java-driver-4.4.13.jar (no standalone netty-*.jar in
#     lib/). CVE-2025-24970 (HIGH), CVE-2026-44249, CVE-2026-45416, CVE-2026-50010,
#     CVE-2024-47535, CVE-2025-25193, CVE-2026-42583, CVE-2025-58057. Fixing needs
#     rebuilding neo4j-java-driver from source (out of scope, MAJOR effort).
#   - MAJOR bump required: org.apache.tika tika-core / tika-parsers 1.28.5 —
#     CVE-2025-66516 (CRITICAL) + CVE-2025-54988 (HIGH); fixed only in tika 2.x/3.x.
#   - MAJOR bump required: dnsjava 2.1.9 — CVE-2024-25638; fixed only in dnsjava 3.6.0.
# NOTE: the Jul-2026 jackson-databind cluster (CVE-2026-54512..54515) IS standalone
# here (jackson-core/-databind-2.16.1.jar in lib/, same-major fix 2.18.8 / 2.18.6),
# i.e. it would be swappable — but it is NOT the blocker; the shaded-netty and
# major-bump tika/dnsjava CVEs above are what block the gate.
#
# jmeter (Apache JMeter): ships the upstream binary distribution (arch-independent
# jars) on a hardened Wolfi JRE, the CVE surface the Trivy gate scans. JMeter
# maintains one active line (5.6.x); we ship the newest release only. Each release
# pins its distribution tarball by sha256. Image-only (a load-testing CLI/tool, no
# chart).
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(5.6.3)
declare -A SHA256=(
  [5.6.3]=f68efc17fe060f698c48a6abe2599a933927486bda2924dbe14c74895318ddde
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
