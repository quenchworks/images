# STATUS: BLOCKED (do not publish) -- 0-CVE Trivy gate FAILS on 3.7.0 (147 fixable
# findings: 43 HIGH, 104 MEDIUM) and CANNOT be cleared without forking/rebuilding
# OpenSearch's OWN opensaml uber-jar. Newest-only already applied (3.7.0 is the newest
# OpenSearch release; 3.7.1/3.7.2/3.8.0/4.0.0 = HTTP 403 on artifacts.opensearch.org,
# verified 2026-07-04). Every finding is the Jul-2026 jackson-databind cluster
# CVE-2026-54512..54518 (fix 2.21.4/2.21.5 on the 2.21.x line, 3.1.4 on the 3.1.x line):
#
#   CVE-2026-54512 HIGH   PolymorphicTypeValidator bypass RCE   fix 2.21.4 / 3.1.4
#   CVE-2026-54513 HIGH   security-bypass arbitrary code exec   fix 2.21.4 / 3.1.4
#   CVE-2026-54514 MEDIUM info disclosure via eager DNS resolve fix 2.21.4 / 3.1.4
#   CVE-2026-54515 HIGH   ignored properties can be modified    fix 2.21.5 / 3.1.4  (NOTE: 2.21.4 does NOT clear this; needs 2.21.5)
#   CVE-2026-54516 HIGH   renamed-property handling bypass      fix 2.21.4 / 3.1.4
#   CVE-2026-54517 MEDIUM JsonView filter info disclosure       fix 2.21.4 / 3.1.4
#   CVE-2026-54518 HIGH   view-based access-control bypass      fix 2.21.4 / 3.1.4
#
#  * DECISIVE BLOCKER (shaded inside a vendor uber-jar, NOT drop-in replaceable):
#    plugins/opensearch-security/opensaml-3.7.0.0-all.jar -- OpenSearch's OWN
#      security-plugin "opensaml-all" fat jar; it SHADES jackson-databind 2.21.3
#      (embedded META-INF/maven/com.fasterxml.jackson.core/jackson-databind/pom.properties,
#      confirmed by unzip). Trivy flags all 7 CVEs off that bundled metadata. Fixing the
#      shaded jackson inside this uber-jar requires rebuilding it upstream = a FORK.
#
#  * standalone module/plugin jars (these ALONE would be drop-in replaceable -- swap
#    2.21.3 -> 2.21.5 and 3.1.3 -> 3.1.4 in the melange step -- but there are ~18 copies
#    AND clearing them still leaves the shaded opensaml copy above, so the gate stays red):
#    modules/ingest-geoip/jackson-databind-2.21.3.jar
#    plugins/opensearch-alerting/jackson-databind-{2.21.3,3.1.3}.jar
#    plugins/opensearch-anomaly-detection/jackson-databind-{2.21.3,3.1.3}.jar
#    plugins/opensearch-flow-framework/jackson-databind-3.1.3.jar
#    plugins/opensearch-ml/jackson-databind-{2.21.3,3.1.3}.jar
#    plugins/opensearch-neural-search/jackson-databind-3.1.3.jar
#    plugins/opensearch-notifications-core/jackson-databind-3.1.3.jar
#    plugins/opensearch-notifications/jackson-databind-2.21.3.jar
#    plugins/opensearch-observability/jackson-databind-3.1.3.jar
#    plugins/opensearch-reports-scheduler/jackson-databind-3.1.3.jar
#    plugins/opensearch-search-relevance/jackson-databind-3.1.3.jar
#    plugins/opensearch-security/jackson-databind-{2.21.3,3.1.3}.jar
#    plugins/opensearch-sql/jackson-databind-2.21.3.jar
#    plugins/opensearch-ubi/jackson-databind-3.1.3.jar
#
# WHY BLOCKED (per gate policy): the fixable jackson CVEs are SHADED inside OpenSearch's
# own opensaml-3.7.0.0-all uber-jar -- not a removable module nor a drop-in-replaceable
# standalone jar -- so clearing them requires a fork/rebuild of that uber-jar. Replacing
# the ~18 standalone jackson-databind jars is possible but does NOT clear the gate on its
# own (the shaded opensaml copy remains). No newer OpenSearch release exists to inherit
# the fix (3.7.0 is newest, verified 2026-07-04) and no OpenSearch build yet bundles
# jackson >= 2.21.5 / 3.1.4. Same root cause as the sibling elasticsearch block.
# Re-evaluate when OpenSearch ships a release bundling jackson-databind >= 2.21.5 / 3.1.4
# (including a rebuilt opensaml-all).
#
# opensearch: from official per-arch distribution tarballs (x64/arm64; they
# bundle native libs) on the hardened openjdk-21-jre. Newest stable release only.
# Per-version, per-arch sha512 (SHA_AMD/SHA_ARM, from the artifacts.opensearch.org
# .tar.gz.sha512 sidecars).
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(3.7.0)
declare -A SHA_AMD=(
  [3.7.0]=9e481db6495cbdacd6704a19c6ed7679e1138d40dbe283642c599bf70441c968bbd7f6faf7ecfa6c9ecf0685141037079b53647c5df3595bd5b9647bcbad1a04
)
declare -A SHA_ARM=(
  [3.7.0]=fbc2dcb98ab909c113f70473861f9a7e62d9b3c59c6c06201617a3e3d1ce7ec9707ecba217ead62ea553f2486bb91b87f2d34a99d498dc5c5d4f0659fe01db50
)
render() { echo "s/__VER__/$1/g; s|__SHA_AMD__|${SHA_AMD[$1]}|g; s|__SHA_ARM__|${SHA_ARM[$1]}|g"; }
