# rust: apko-runtime from Wolfi's rust-<minor> toolchain. Published tag = FULL
# version Wolfi resolves (latest 3 lines); render maps __VER__ -> the minor.
FROM_SOURCE=0
VERSIONS=(1.94.1 1.95.0 1.96.0)
render() { local v="$1"; echo "s/__VER__/${v%.*}/g"; }
