# perl: the Perl interpreter from Wolfi (FROM_SOURCE=0, apko-only, 0-CVE).
# Latest 3 stable releases, each pinned to its exact Wolfi apk; tag = X.Y.Z.
FROM_SOURCE=0
VERSIONS=(5.42.0 5.42.1 5.42.2)
declare -A PIN=( [5.42.0]=5.42.0-r2 [5.42.1]=5.42.1-r1 [5.42.2]=5.42.2-r3 )
render() { echo "s|__PKG__|perl=${PIN[$1]}|g"; }
