# grafana-alloy: from-source via melange (Go build from the git tag tarball).
# Image-only (an OpenTelemetry Collector distribution / telemetry agent, no chart
# here). Latest patch of the last 3 minor lines (1.15, 1.16, 1.17). Alloy embeds a
# React web UI (go:embed via the `embedalloyui` build tag), so the melange build
# compiles that UI with npm first, then the single Go binary. All three releases
# use the SAME build recipe (GO_TAGS "netgo embedalloyui", version stamped into
# github.com/grafana/alloy/internal/build.Version), so a single __VER__ +
# per-version source-tarball __SHA256__ render suffices. Upstream tags carry a
# "v" prefix; the release tarball is v${VER}.tar.gz.
FROM_SOURCE=1
VERSIONS=(1.15.1 1.16.3 1.17.1)
declare -A SHA256=(
  [1.15.1]=1b3d30343093a2dfd35d7d1decf61fe74fb01dc2473dde779cc1008cd6ceaf31
  [1.16.3]=ed43108bca6cbae25c130e16a229f517b3c66e0f22d3ade39aaddaae2639f490
  [1.17.1]=a3072c30a70901764bb31f26c7fa126cf1f625ac5c34e7cd407fcefad8c8f461
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }

# STATUS 2026-08-27 -- STILL BLOCKED, and the recorded reason is essentially
# right, though it names the wrong CVE and the wrong single importer.
#
# WHAT BLOCKS IT. github.com/docker/docker is pinned to the v28 line for everyone:
# proxy.golang.org returns v28.5.2+incompatible as @latest for BOTH
# github.com/docker/docker and github.com/moby/moby (verified today), while the
# v29 code ships only as the split modules github.com/moby/moby/api and
# github.com/moby/moby/client (which do not contain the flagged code) plus
# github.com/moby/moby/v2 pre-release betas. So the "fixed in 29.3.1" version
# Trivy names cannot be obtained on the github.com/docker/docker module path at
# all -- there is nothing to float to. Same wall as apps/coolify-helper.
#
# It is NOT a test-only link. Unlike hydra/kratos (where one vendored ory/x test
# helper was the entire cause and deleting it cleared the gate), alloy links
# docker/docker from production code. `go list -deps` on the real collector/
# module with the real build tags ("netgo embedalloyui") at 1.17.1 shows
# github.com/docker/docker/client and github.com/docker/docker/api/types/* in the
# binary's import graph, reached from FOUR packages:
#   github.com/prometheus/prometheus/discovery/moby          (discovery.docker /
#                                                             discovery.dockerswarm)
#   github.com/google/cadvisor/container/docker               (prometheus.exporter.cadvisor)
#   github.com/google/cadvisor/container/docker/utils                 "
#   .../opentelemetry-collector-contrib/internal/metadataproviders/docker
#                                                             (resourcedetection docker)
# Two of those arrive through Grafana's own hard `replace` pins:
#   replace github.com/google/cadvisor    => github.com/grafana/cadvisor v0.0.0-20260204200106-...
#   replace github.com/prometheus/prometheus => github.com/grafana/prometheus v1.8.2-0.20260313...
# and a `replace` outranks `go get` (docs/go-module-float.md #1), so no float can
# move them. No build tag gates these components either: the only `nodocker` tags
# in the tree are on _test.go files, so there is no docker-free build variant to
# flip (the trick that worked on `pack`).
#
# WHERE THE ECOSYSTEM ACTUALLY IS (checked today, useful for the next attempt):
#   * prometheus v0.313.2 discovery/moby  -> ALREADY on github.com/moby/moby/client
#   * otel-collector-contrib main         -> ALREADY on github.com/moby/moby/client
#     (v0.147.0, which 1.17.1 pins, is not)
#   * google/cadvisor master              -> ALREADY on github.com/moby/moby/client
#   * grafana/cadvisor grafana-v0.54.1-noglobals (the pinned fork) -> still
#     github.com/docker/docker/client + api/types/system
#   * alloy v1.19.2 (newest, 2026-08-26) drops the grafana/prometheus replace and
#     takes prometheus v0.313.2 unforked, but KEEPS the grafana/cadvisor replace
#     and still requires github.com/docker/docker v28.5.2+incompatible.
# So the remaining holdout is Grafana's cadvisor fork. This unblocks when Grafana
# rebases that fork onto a moby-v29 cadvisor or drops it -- not on our side.
#
# WHAT I COULD NOT VERIFY. The local build does not fit the 3500M memory cap this
# work runs under: the Go `compile` of alloy's collector module was OOM-killed by
# the cgroup twice ("Memory cgroup out of memory: Killed process ... (compile)
# total-vm:4180088kB, anon-rss:2800240kB", scope memory peak 3.4G), so I have NO
# Trivy table from today for this app -- the blocker above is proven from the
# import graph and the module proxy, not from a scan I ran. Whatever ordinary apk
# and Go module drift has accumulated since it was last built is therefore also
# unmeasured, and this recipe still carries NO module float at all, so expect the
# canonical x/* + grpc + otel floors to be needed on top once docker clears.
BLOCKED=1
