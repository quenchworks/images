# BLOCKED 2026-08-17 -- and the PUBLISHED 7.1.7 IMAGE IS BROKEN, do not use it.
#
# What happened: overlaying httpcomponents5 (httpclient5 5.5.1 -> 5.6.3, httpcore5 and
# httpcore5-h2 5.3.4 -> 5.4.3) into plugin/graylog-storage-opensearch3 DID reach 0-CVE,
# but it breaks graylog at runtime. Verified in kind with healthy MongoDB + OpenSearch:
# graylog CrashLoopBackOffs with
#   java.util.zip.ZipException: Not in GZIP format
#     at org.opensearch.client.json.jackson.JacksonJsonProvider$ParserFactory.createParser
# i.e. the newer client/core pair changes content-encoding handling and the OpenSearch
# client can no longer decode the response body. The 0-CVE gate AND the image smoke test
# both passed -- only the CHART install gate caught it.
#
# The httpcomponents5 overlay is therefore REVERTED. log4j-api 2.26.1 and jsoup 1.23.1
# (shaded in graylog.jar) are KEPT: verified safe, the JVM boots and logs normally and
# neither sits on the OpenSearch path.
#
# Residual, unfixable without a compatible pair: httpclient5 5.5.1, httpcore5 5.3.4,
# httpcore5-h2 5.3.4. UNBLOCK by finding a httpcomponents5 combination that both clears
# the advisories and preserves the gzip behaviour the bundled OpenSearch client expects
# (try httpclient5 5.5.x patch releases first, keeping httpcore5 on 5.3.x), or wait for
# upstream graylog to bump the storage plugin itself.
#
# ACTION OWED: the graylog:7.1.7 tag in GHCR currently serves the BROKEN build (digest
# 95f844acb81b...). It must be republished from a fixed recipe, or withdrawn. No chart
# points at it -- the chart re-pin release failed, so published charts still reference
# the previous good digest.
BLOCKED=1
# STATUS: SHIPPED (0-CVE) -- UNBLOCKED 2026-07-09 via CLASS-LEVEL SURGERY inside the
# shaded uber-jars. The prior "shaded, not swappable" verdict was based on there being
# no standalone lib jar -- but the shaded classes are LOOSE and UN-RELOCATED (plain
# package paths), so they ARE overlay-swappable. Gate passes; the image boots and
# reports `Graylog 7.1.x` (jar classloads + initializes). Bundled and surgically patched:
#   jackson-databind 2.21.2 -> 2.21.5 (CVE-2026-54512..54518; 2.21.5 needed for -54515;
#     HIGH ACE). jackson-core bumped 2.21.2 -> 2.21.5 in lockstep to match databind ABI.
#   org.apache.shiro:shiro-core 2.1.0 -> 2.2.1 (CVE-2026-49268 HIGH LDAP injection,
#     CVE-2026-43827 MEDIUM session fixation) -- the WHOLE 9-module shiro family
#     (core/cache/config-core/config-ogdl/crypto-cipher/crypto-core/crypto-hash/event/
#     lang) bumped in lockstep so the minor stays internally consistent.
#   com.sun.mail:jakarta.mail 1.6.7 -> 1.6.8 (CVE-2025-7962 MEDIUM SMTP injection).
#   at.yawk.lz4:lz4-java 1.10.4 -> 1.11.1 (CVE-2026-59949 MEDIUM, native XXHash can
#     crash the JVM) -- added for 7.1.6. Sole artifact in the at.yawk.lz4 group, so
#     there is no sibling family to keep aligned, and only graylog.jar bundles it
#     (all three storage plugin jars are lz4-java-free). The bundled net/jpountz tree
#     is the exact 1.10.4 file set and 1.11.1's is a strict superset, so the overlay
#     leaves nothing stale; the prebuilt liblz4-java.so natives live in that same tree
#     and are replaced along with the classes, which is where the crash actually is.
# All are exploded as loose .class files + META-INF/maven/.../pom.properties directly
# inside graylog.jar (~299MB Multi-Release fat jar) AND the graylog-storage-opensearch3
# plugin jar. TECHNIQUE (see melange.yaml): explode each uber-jar, OVERLAY the patched
# upstream class trees + their maven pom.properties (sha256-pinned Maven Central), and
# repackage. Not relocated => the patched classes drop in at identical package paths.
#
# TRADE-OFFS / caveats: the image smoke exercises jar classload + `version`; the shiro
# auth path and SMTP alerting run under MongoDB + a search store, which are deferred to
# the chart's kind install gate (Graylog cannot fully boot without them -- unchanged
# posture). shiro 2.1->2.2 and jackson/mail patch bumps are same-major, low-risk. If a
# future Graylog release bundles jackson-databind >= 2.21.5 / shiro >= 2.2.1 / mail >=
# 1.6.8 / lz4-java >= 1.11.1 natively, this surgery becomes a no-op and can be dropped.
#
# graylog: ships the upstream official tarball (graylog-<ver>.tgz from
# packages.graylog2.org) on a hardened Wolfi JRE, the CVE surface the Trivy gate
# scans. The tarball is graylog.jar (a Multi-Release fat jar) + bin/graylogctl +
# plugin/*.jar -- pure Java, architecture-independent, so ONE sha256 per version
# (the arch comes entirely from the JRE, jenkins-style). We ship the newest patch
# of each of the last 3 stable minor lines (6.3.x, 7.0.x, 7.1.x).
#
# JRE: 6.3.x is compiled for Java 17 (bytecode 61), 7.0/7.1 for Java 21 (bytecode
# 65). Java 21 runs 17 bytecode, so openjdk-21-jre covers all three lines.
#
# LICENSE = SSPL-1.0 (Server Side Public License) -- SOURCE-AVAILABLE, NOT OSI
# approved. caution tier + a loud note in the catalog. Server app: image + chart.
FROM_SOURCE=1
# newest-only: keep just the newest patch of the newest stable line (7.1.6).
VERSIONS=(7.1.7)
declare -A SHA256=(
  [7.1.7]=9b620a8db19227fc7ad1d161144f08b28f82c777ec030f97c3ce83e7d2411a28
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
