# STATUS: BLOCKED (do not publish)
# The 0-CVE Trivy gate FAILS on the newest release (v4.15.3) with 37 fixable CVEs,
# ALL in the upstream prebuilt gobinary (usr/bin/zitadel). This recipe is FROM_SOURCE=0:
# it ships ZITADEL's OWN official prebuilt static Go binary from the GitHub release.
# There is NO local Go module tree here, so `go get <mod>@<fixed> && go mod tidy` CANNOT
# be applied -- the flagged modules AND the Go toolchain are baked into upstream's binary.
#
# A from-source conversion was investigated in depth (2026-07-09) and is INFEASIBLE for
# v4.15.3. The Go DEPENDENCY bumps themselves are trivial (gorilla/csrf@v1.7.3,
# golang.org/x/net@v0.56.0, golang.org/x/crypto@v0.53.0 all resolve; stdlib clears via
# go-1.26.5) -- but they are MOOT because the source tarball cannot be compiled:
#   1. GENERATED CODE IS GITIGNORED, NOT SHIPPED. .gitignore excludes `**.pb.go` and
#      `console/src/app/proto/generated`, so the release source tarball
#      (archive/refs/tags/v4.15.3.tar.gz) contains NO generated gRPC Go stubs. `go build .`
#      / `go mod tidy` FAIL immediately: e.g. self-package
#      `github.com/zitadel/zitadel/pkg/grpc/analytics/v2beta` does not exist (only 1 of the
#      ~23 pkg/grpc packages has committed .go). A build must first reproduce the ENTIRE
#      buf codegen pipeline: protoc-gen-{go,go-grpc,grpc-gateway,openapiv2,validate,
#      connect-go} PLUS ZITADEL's two CUSTOM in-repo plugins (internal/protoc/
#      protoc-gen-authoption, protoc-gen-zitadel) PLUS BSR proto deps pulled from buf.build
#      (grpc-ecosystem/grpc-gateway, envoyproxy/protoc-gen-validate, googleapis/googleapis).
#      This is NOT the mattermost "server-only go build" pattern -- mattermost's server Go
#      is fully committed; ZITADEL's server Go is itself generated and absent.
#   2. THE `zitadel-login.tar.gz` RELEASE ASSET IS NOT A go:embed ASSET. It is the
#      standalone login-v2 Next.js NODE service (ships .next/, server.mjs, node_modules/,
#      healthcheck.mjs) that runs as a SEPARATE runtime container -- it is not embedded in
#      the Go binary and cannot be "dropped where go:embed expects." The actual go:embed
#      target is the ANGULAR console at internal/api/ui/console/static, which is EMPTY in
#      source (just `gitkeep`); CI populates it from `console/dist/console` via a full
#      Angular 21 `ng build` + its own buf codegen (protoc-gen-js, protoc-gen-grpc-web,
#      protoc-gen-openapiv2 -- none packaged in Wolfi). No release asset ships the built
#      console; it exists only baked inside upstream's STRIPPED prebuilt binary (confirmed:
#      embeds static/chunk-*.js), and go:embed extraction from a stripped Go binary is
#      unreliable. Building it is exactly the heavy Angular frontend rebuild (OOM risk) to
#      be avoided, and shipping with an empty console = broken admin UI.
# v4.15.3 is already the newest v4 release (v5.0.0-base is a pre-release base tag), so there
# is no newer patch to adopt. -> BLOCKED until upstream ships a release compiled with the
# patched deps + Go toolchain (or publishes a from-source-buildable server without the
# gitignored-codegen + Angular-console requirements).
#
# Flagged (module  installed -> fixed  : representative CVEs):
#   github.com/gorilla/csrf  v1.7.2  -> 1.7.3           : CVE-2025-24358
#   golang.org/x/crypto      v0.50.0 -> 0.52.0          : CVE-2026-39827/39828/39829/39830/
#                                                          39831/39832/39833/39834/39835/
#                                                          42508/46595/46597/46598 (ssh)
#   golang.org/x/net         v0.53.0 -> 0.55.0          : CVE-2026-25680/25681/27136/39821/
#                                                          42502/42506 (html,idna)
#   stdlib (Go toolchain)    v1.25.8 -> 1.25.11/1.26.4  : CVE-2026-27145/32280/32281/32282/
#                                                          32283/32288/32289/33811/33814/
#                                                          39820/39823/39825/39826/39836/
#                                                          42499/42504/42507 (crypto/x509,
#                                                          crypto/tls,net,net/mail,net/http,
#                                                          html/template,archive/tar,textproto)
#
# zitadel: image-only recipe input (the chart is authored in the charts repo).
# ZITADEL is a Go IAM/OIDC server with an embedded Angular console baked into the
# binary via go:embed; a from-source build needs a Node/Angular toolchain and is
# infeasible in CI. Instead we ship ZITADEL's OWN official prebuilt server binary
# from the GitHub release (the same upstream-artifact pattern clickhouse/mattermost
# use), then harden it on a minimal apko base and gate it 0-CVE with Trivy. This is
# clean-room-consistent: it is upstream's published artifact, not a Bitnami chart.
#
# Versions = newest v4 patch only (trimmed from the last-3-minors set to newest-only).
# Release tags are vX.Y.Z; the asset filename carries NO version, only the arch:
#   https://github.com/zitadel/zitadel/releases/download/v<VER>/zitadel-linux-<arch>.tar.gz
# where <arch> is amd64 or arm64. Each version ships an amd64 and an arm64 tarball,
# each with its own sha256:
#   __VER__     = version (bare X.Y.Z; the leading `v` is added in the fetch URL)
#   __SHA_AMD__ = zitadel-linux-amd64.tar.gz sha256
#   __SHA_ARM__ = zitadel-linux-arm64.tar.gz sha256
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=0
VERSIONS=(4.15.3)
declare -A SHA_AMD=(
  [4.15.3]=5efc4eea7e4bf11d193cedfe976a1aa32e0d15b9076897460ac6a4a01290a6d0
)
declare -A SHA_ARM=(
  [4.15.3]=558f799175bcba00e33617ec9211cdc4826eda8de3343ea54440eb2a2eb0df2f
)
render() {
  echo "s/__VER__/$1/g; s|__SHA_AMD__|${SHA_AMD[$1]}|g; s|__SHA_ARM__|${SHA_ARM[$1]}|g"
}
