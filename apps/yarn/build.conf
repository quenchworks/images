# yarn: Wolfi yarn package on nodejs-22. VERSIONS = Yarn major line (tag);
# apko carries __YARNPKG__ = the Wolfi package per tag (1 -> yarn).
FROM_SOURCE=0
VERSIONS=(1)
render() { case "$1" in 1) echo "s/__YARNPKG__/yarn/g";; *) echo "s/__YARNPKG__/yarn-$1/g";; esac; }
