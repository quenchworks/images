# composer: composer layered on a fixed PHP minor. VERSIONS = Composer line (tag);
# apko only carries __PHPVER__ = fixed PHP 8.4 (the workflow's phpver).
FROM_SOURCE=0
VERSIONS=(2)
render() { echo "s/__PHPVER__/8.4/g"; }
