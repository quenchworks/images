# re-gated 2026-07-09 (fresh Trivy DB, live Wolfi APKINDEX, go-1.26.5): STILL BLOCKED.
# STATUS: BLOCKED (do not publish). Bumped to the newest stable tag v0.42.0 (was
# 0.41.0); the build assembles cleanly, but the comprehensive Trivy gate flags
# fixable CVEs in the github.com/prometheus/prometheus dependency that thanos pins.
# v0.42.0 pins prometheus v0.309.1 (v0.41.0 pinned v0.308.0); both are < the fix line.
#   CVE-2026-42151 (HIGH, Azure OAuth secret disclosure via config API)
#   CVE-2026-42154 (HIGH, remote-read DoS via uncontrolled memory allocation)
#   CVE-2026-44903 (HIGH)
#   CVE-2026-40179 (MEDIUM, stored XSS via metric names/label values)
# All four are fixed ONLY in prometheus >= 0.311.3.
#
# ROOT-CAUSE ATTEMPT (2026-07-09, per policy): tried the go-get + go mod -replace
# route to force prometheus v0.311.3 and patch the call sites. It is NOT a few files:
# the 0.309 -> 0.311.3 jump is a PERVASIVE, ecosystem-wide promql/tsdb API break, and
# upstream thanos itself is still on prometheus v0.309.1 on `main` (no migration to
# copy). Enumerated compile breaks after forcing v0.311.3 (exact symbols):
#   * github.com/prometheus/prometheus/tsdb/errors package REMOVED (NewMulti, CloseAll)
#     -- used by 4 thanos files; shimmable (prometheus moved to stdlib errors.Join).
#   * promql parser refactor: parser.NewParser now takes (parser.Options) not
#     (string, ...); parser.WithFunctions removed; parser.Parser.Close removed;
#     parser.ParseExpr / parser.ParseMetricSelector are no longer package funcs
#     (now interface methods) -- breaks pkg/extpromql/parser.go.
#   * promql.BucketQuantile now returns 6 values (was 3);
#     annotations.NewHistogramQuantileForcedMonotonicityInfo signature grew to
#     (string, posrange.PositionRange, int64, float64, float64, float64).
#   * model/relabel.Process REMOVED (pkg/alert/alert.go).
#   * chunkenc.Iterator gained a required AtST() method (created-timestamp support)
#     -- 3 iterator impls in internal/cortex/querier/series break.
#   * THIRD-PARTY deps still pinned to the OLD prometheus API and would each need a
#     new upstream release: github.com/thanos-io/promql-engine (BucketQuantile /
#     annotations), github.com/prometheus-community/prom-label-proxy@v0.11.1
#     (parser.ParseExpr / parser.ParseMetricSelector).
# This is a MAJOR/API-break chain gated on upstream thanos (+ promql-engine +
# prom-label-proxy) moving to prometheus >= 0.311.3. Per policy: hard BLOCK, do NOT
# force the chain. The other flagged deps (go-jose/v4, otel family, x/crypto, x/net,
# mongo-driver) are individually clearable but MOOT while the prometheus CVEs remain.
# UNBLOCK when upstream thanos ships a release built on prometheus >= 0.311.3.
#
# thanos: from-source via melange (Go build, git-checkout pinned per release).
# Each release pins its git commit (the dereferenced commit the annotated tag points at).
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(0.42.0)
declare -A COMMIT=(
  [0.42.0]=0d7a3536be5385d8dec8d36b77ad5179c800348c
)
render() { echo "s/__VER__/$1/g; s/__COMMIT__/${COMMIT[$1]}/g"; }
