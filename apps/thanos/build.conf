# STATUS: BLOCKED (do not publish). The build assembles cleanly and 0.41.0 is the
# newest stable tag, but the comprehensive Trivy gate flags fixable CVEs in the
# vendored github.com/prometheus/prometheus v0.308.0 that thanos pins:
#   CVE-2026-42151 (HIGH, Azure OAuth secret disclosure via config API)
#   CVE-2026-42154 (HIGH, remote-read DoS via uncontrolled memory allocation)
#   CVE-2026-44903 (HIGH)
#   CVE-2026-40179 (MEDIUM, stored XSS via metric names/label values)
# All four are fixed ONLY in prometheus >= 0.311.3 (the 0.305.2 branch in the
# advisory is a patch on an older minor line and does not apply to 0.308.x). Bumping
# prometheus 0.308 -> 0.311.3 forces k8s.io/apimachinery v0.35 and a breaking
# textparse API change that thanos 0.41.0 does not support -- a MAJOR/API-break
# chain, not a minimal in-major bump. Per policy this is a hard BLOCK: do NOT force
# the prometheus-0.311.3 chain. The other flagged deps (go-jose/v4 -> 4.1.4,
# otel family -> 1.41-1.43, golang.org/x/crypto -> 0.52.0, golang.org/x/net ->
# 0.55.0, go.mongodb.org/mongo-driver -> 1.17.7) are individually clearable, but the
# prometheus CVEs cannot be, so the 0-fixable-CVE gate is unreachable for thanos
# until upstream thanos itself moves to prometheus >= 0.311.3.
#
# thanos: from-source via melange (Go build, git-checkout pinned per release).
# Latest 3 stable minor lines, newest patch each. Each release pins its git commit
# (the dereferenced commit the annotated vX.Y.Z tag points at).
FROM_SOURCE=1
VERSIONS=(0.41.0)
declare -A COMMIT=(
  [0.41.0]=cb1396b916241f63fff75e4f5362ea65f18f2303
)
render() { echo "s/__VER__/$1/g; s/__COMMIT__/${COMMIT[$1]}/g"; }
