# STATUS: BLOCKED (do not publish) -- re-verified 2026-08-31
# ===========================================================================
# ⚠️  CORRECTION TO THE 2026-08-27 NOTE: THIS IMAGE *IS* PUBLISHED, AND THE
#     PUBLISHED ARTIFACT IS NOT CLEAN. The previous note asserted the exact
#     opposite ("NOT published, ever ... its absence from the nightly
#     published-image scan means 'never published', not clean"). That was
#     FALSE, and it hid a live, PUBLIC supply-chain exposure for four days.
#     Do not restore that claim without re-running the two commands below.
#
# PROOF IT IS PUBLISHED (2026-08-31):
#   $ gh api /orgs/quenchworks/packages/container/images%2Fcoolify-helper/versions
#     -> tag "1.0.14"   sha256:3ac8c059ddec50582bb80d6f9626f2e2d06fd7ff67b99f8204976fe3cfd5815b
#        created_at 2026-06-28T22:13:12Z  (plus 1.0.14-amd64 / 1.0.14-arm64 and a
#        cosign .sig, i.e. it went through the full publish+sign path)
#   $ gh api /orgs/quenchworks/packages/container/images%2Fcoolify-helper
#     -> "visibility": "public"           <-- ANYONE can pull it
#
# WHY THE OLD NOTE GOT IT WRONG -- TWO COMPOUNDING MISTAKES:
#   1. IT QUERIED THE WRONG NAME. It looked for the GHCR package
#      `quench-coolify-helper` -- that is the MELANGE PACKAGE name, not the image
#      path. GHCR images live at `images/<app>`, so `images%2Fcoolify-helper`.
#      The 404 it saw was a name error, not an absence.
#   2. IT TREATED SCAN ABSENCE AS EVIDENCE OF NON-PUBLICATION. It cannot be:
#      scripts/gen-catalog.py filters `status: blocked` rows OUT of
#      catalog.lock.yaml ("status: blocked == delisted", gen-catalog.py:~205),
#      and the nightly published-image scan reads the lock. So marking this app
#      blocked is *precisely what removed it from the scan*. The reasoning was
#      circular: blocked -> dropped from lock -> unscanned -> "never published"
#      -> stays blocked. A blocked app's published artifacts are a SCAN BLIND
#      SPOT. Absence from the nightly is never evidence of anything.
#
# THE PUBLISHED DIGEST IS BADLY DRIFTED (scanned 2026-08-31, fresh DB, with the
# gate's own flags: --ignore-unfixed --severity CRITICAL,HIGH,MEDIUM,LOW
# --scanners vuln --detection-priority comprehensive):
#     sha256:3ac8c059... = 282 FIXABLE  (203 HIGH / 70 MEDIUM / 9 LOW)
#   Of those 282 rows, 75 are the bundled-apk-SBOM duplicates under
#   var/lib/db/sbom/, so ~207 are distinct. By target:
#     usr/local/bin/pack                      62
#     usr/local/bin/railpack                  43
#     wolfi OS layer                          27   (busybox/openssl, June apks)
#     usr/bin/mc                              19
#     usr/libexec/.../docker-buildx           19
#     usr/bin/docker-compose                  16
#     usr/bin/git-lfs                         12
#     usr/bin/docker                           9
#   Top packages: stdlib 118 (Go-toolchain drift), x/net 36, x/crypto 29,
#   buildkit 14, x/text 10, go-git/v5 10, grpc 7, docker/docker 6.
#   This is 1.0.14 built 2026-06-28 with June apks (docker-cli 29.6.1-r0,
#   compose 5.2.0-r0, buildx 0.35.0-r1, git-lfs 3.7.1-r16) -- i.e. ordinary
#   "no rebuild cron -> published images drift", not a new defect. NOTE the
#   published tag is 1.0.14 while VERSIONS below is 1.0.15: the published
#   artifact is a version BEHIND this recipe.
#
# AND IT IS ADVERTISED BY A RELEASED CHART: charts/quench/coolify/Chart.yaml
#   pins that exact digest under `artifacthub.io/images`. The chart does NOT
#   deploy it (no values.yaml entry, no template reference -- the helper runs on
#   the EXTERNAL Docker hosts Coolify drives over SSH), so `helm install` does
#   not pull it. But it is published, PUBLIC, listed on ArtifactHub as one of
#   this chart's images, and therefore the digest we tell users is the helper.
#   That is a real exposure, and the old note's "never published" is what kept
#   it off every remediation list.
#
# ---------------------------------------------------------------------------
# THE BLOCKER ITSELF -- RE-VERIFIED 2026-08-31, STILL TRUE:
#   github.com/docker/docker v28.5.2+incompatible, CVE-2026-33997 (MEDIUM,
#   "Moby: privilege validation bypass during plugin installation"). Trivy's
#   fixed version is 29.3.1, which DOES NOT EXIST as a reachable Go module:
#     docker/docker  @latest -> v28.5.2+incompatible  (2025-11-05)
#     moby/moby      @latest -> v28.5.2+incompatible  (2025-11-05)
#     moby/moby/v2   @latest -> v2.0.0-beta.21        (prerelease only)
#     the 29.x line ships only as the SPLIT modules moby/moby/api v1.55.0 and
#     moby/moby/client v0.5.1 -- neither carries the flagged plugin-install code.
#   So no `go get` can reach 29.3.1. Same wall as apps/gitness.
#
#   It enters through ONE upstream import, re-checked at the CURRENT release:
#     github.com/docker/buildx v0.36.1  store/util.go:8
#       imports github.com/docker/docker/pkg/namesgenerator  (used once, to
#       auto-name a builder). Verified present in v0.36.1 today.
#     docker-compose inherits it transitively via buildx/store/storeutil;
#     compose's own tree has zero docker/docker imports.
#   buildx v0.36.1 (2026-08-04) and compose v5.5.0 (2026-08-17) are STILL the
#   newest upstream releases, and both reach us as prebuilt Wolfi apks we do not
#   compile. Clearing it would mean shipping our own from-source buildx+compose
#   with buildx's name generator swapped out -- a behavioural fork of upstream
#   for a MEDIUM in a random-name generator. Deliberately not done.
#
# WOLFI DID MOVE SINCE 2026-08-27 (so a rebuild helps, but cannot clear it):
#   docker-compose 5.5.0-r2 -> 5.5.0-r3, git-lfs 3.7.1-r21 -> 3.8.0-r0,
#   busybox 1.38.0-r1 -> -r2; docker-cli 29.7.2-r3, docker-cli-buildx 0.36.1-r2
#   and mc r21 UNCHANGED. Because buildx's apk is unchanged, the docker/docker
#   row survives any rebuild.
#
# UNBLOCK CONDITION (any one of):
#   1. github.com/docker/buildx stops importing docker/docker/pkg/namesgenerator
#      (then Wolfi's next compose+buildx rebuild clears CVE-2026-33997), or
#   2. the moby 29.x daemon is published on a real, non-prerelease Go module
#      path so a float can reach 29.3.1.
#
# WHAT TO DO IN THE MEANTIME -- THIS IS NOT "NOTHING":
#   The block is correct for PUBLISHING a new 1.0.15. It is NOT a reason to
#   leave a public, 282-CVE 1.0.14 standing and unadvertised-as-drifted. Either
#   delete/deprecate the published 1.0.14 package version, or drop it from the
#   coolify chart's artifacthub.io/images list, or both. Whoever owns publishing
#   must decide; this recipe cannot.
#
# RECORDED 2026-08-27, NOT RE-VERIFIED TODAY (kept so the next pass is
# mechanical, not re-derived -- treat as leads, re-check before relying on them):
#   * golang.org/x/mod v0.38.0 -> 0.40.0 (CVE-2026-56864/-56865, HIGH) in the
#     docker-compose and docker-cli-buildx apks. Wolfi self-heal, or from-source.
#   * mc apk r21: 7x golang.org/x/net v0.54.0 (fix 0.55.0/0.56.0). Wolfi lag
#     ONLY -- upstream mc RELEASE.2025-08-13T08-35-41Z pins x/net v0.42.0, so a
#     from-source build with the house float clears it (pattern: the git-lfs step
#     in apps/coolify-app, the cloudflared step in apps/coolify-realtime -- build
#     here, drop the apk from apko.yaml, which also removes its
#     var/lib/db/sbom duplicate).
#   * git-lfs: apps/coolify-app already builds 3.7.1 from source for exactly this
#     CVE class and can be copied verbatim (note Wolfi is now at 3.8.0-r0).
#   * pack 0.38.2: 51 ordinary floors -- x/net v0.41.0, x/crypto v0.39.0,
#     x/text v0.26.0, x/mod v0.25.0, go-git/v5 v5.16.2 -> 5.19.2, go-billy/v5
#     v5.6.2 -> 5.9.0, buildkit v0.22.0 -> 0.31.1, moby/go-archive v0.1.0 ->
#     0.3.0, circl v1.6.1 -> 1.6.3, selinux v1.12.0 -> 1.13.0, otel v1.36.0 ->
#     1.41.0, docker/cli v28.3.0 -> 29.2.0.
#     Plus docker/docker CVE-2026-33997 -- and in pack it IS removable without a
#     fork: pack's docker/docker comes from ONE file,
#     pkg/client/process_volumes.go (`//go:build linux || windows`), which uses
#     docker/docker/volume/mounts as the -v mount-spec parser. Upstream pack
#     ships its OWN alternative implementation of the same function in
#     pkg/client/process_volumes_unix.go (`//go:build unix && !linux`) built on
#     docker/cli/cli/compose/loader, with no docker/docker. Flipping the two
#     build tags (linux||windows -> windows, unix && !linux -> unix) makes the
#     linux binary use upstream's own docker/docker-free path -- no vendoring,
#     no hand-written parser.
#   * railpack 0.23.0: 33 ordinary floors -- x/net v0.52.0, x/crypto v0.49.0,
#     x/text v0.35.0, x/mod v0.38.0, containerd/v2 v2.2.2 -> 2.3.2, buildkit
#     v0.28.1 -> 0.31.1, grpc v1.79.3 -> 1.82.1, in-toto-golang v0.10.0 ->
#     0.11.0, otel/sdk v1.42.0 -> 1.43.0. railpack does NOT carry
#     docker/docker (`go mod why` answers "main module does not need"). Newer
#     railpack v0.38.0 sits at x/net 0.58.0 / x/crypto 0.55.0 / buildkit 0.32.2
#     with no docker/docker, but it also moves railpack's pinned mise to
#     2026.8.13 -- the two MUST move in lockstep (see the RAILPACK <-> MISE
#     COUPLING note in melange.yaml) and upstream coolify-helper still pins
#     railpack 0.23.0 / mise 2026.3.17.
#   * cloudflared is NOT in this image (that is the sibling coolify-realtime).
# ===========================================================================
# coolify-helper: from-source via melange; version pinned in melange.yaml. No render placeholders.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(1.0.15)
render() { echo "s/__VER__/$1/g"; }