# STATUS: BLOCKED (do not publish) -- re-gated 2026-08-27
# ---------------------------------------------------------------------------
# Full local build (melange x86_64 + apko + Trivy comprehensive, --ignore-unfixed,
# CRITICAL/HIGH/MEDIUM/LOW, fresh DB). The BUILD SUCCEEDS; the GATE FAILS with 99
# fixable findings (report also duplicates 14 of them off the bundled apk SBOMs
# under var/lib/db/sbom/, so the printed report is 113 rows):
#   image.tar (wolfi)                                0
#   usr/bin/docker            docker-cli 29.7.2-r3   0
#   usr/bin/docker-compose    5.5.0-r2               3
#   usr/libexec/.../docker-buildx 0.36.1-r2          3
#   usr/bin/mc                r21                    7
#   usr/bin/git-lfs           3.7.1-r21              1
#   usr/local/bin/pack        0.38.2 (from source)  52
#   usr/local/bin/railpack    0.23.0 (from source)  33
#
# WHAT WAS NEVER TRUE / IS NO LONGER TRUE IN THE OLD NOTES:
#   * NOT published, ever. GHCR has no quench-coolify-helper package (API 404) and
#     catalog.yaml carries `status: blocked` with no digest. Its absence from the
#     nightly published-image scan means "never published", not "clean".
#   * The OS layer SELF-HEALED: busybox 1.38.0-r1 and openssl/libcrypto3 3.6.4-r0
#     are live, so the wolfi target is now 0 (was 7 on the 2026-08-16 tree).
#   * docker-compose advanced 5.3.1-r1 -> 5.5.0-r2 and docker-cli-buildx
#     0.35.0-r3 -> 0.36.1-r2 -- which are exactly the versions upstream
#     coolify-helper (1.0.16) pins, and the NEWEST upstream releases of both
#     projects. CVE-2026-34040 (HIGH) is gone from both.
#   * railpack does NOT carry github.com/docker/docker (the 2026-07-15 note said it
#     did). `go mod why -m github.com/docker/docker` in railpack v0.23.0 answers
#     "main module does not need module github.com/docker/docker"; all 33 of its
#     rows are ordinary module floors.
#   * cloudflared is NOT in this image at all (that is the sibling coolify-realtime).
#     apko.yaml has no cloudflared and the recipe never builds one.
#
# THE ACTUAL BLOCKER -- github.com/docker/docker v28.5.2+incompatible,
# CVE-2026-33997 (MEDIUM, "Moby: privilege validation bypass during plugin
# installation"). Trivy's fixed version is 29.3.1. THAT VERSION DOES NOT EXIST AS A
# GO MODULE, on any path:
#     $ curl -s https://proxy.golang.org/github.com/docker/docker/@latest
#       {"Version":"v28.5.2+incompatible", ...}
#     $ curl -s https://proxy.golang.org/github.com/moby/moby/@latest
#       {"Version":"v28.5.2+incompatible", ...}
#   The moby v29 line ships only as the SPLIT modules github.com/moby/moby/api and
#   github.com/moby/moby/client (neither contains the flagged code) and as the
#   pre-release github.com/moby/moby/v2 v2.0.0-beta.*. So NO `go get` can reach
#   29.3.1 -- the same wall apps/gitness is held behind.
#   It enters this image through the two Wolfi CLI-plugin apks, and only through
#   ONE import in one upstream file:
#     github.com/docker/buildx  store/util.go:8  imports
#     github.com/docker/docker/pkg/namesgenerator  (used once, in GenerateName(),
#     to auto-name a builder). That is buildx's ONLY docker/docker import.
#     docker-compose inherits it transitively -- `go mod why` in compose v5.5.0:
#       github.com/docker/compose/v5/pkg/compose
#       github.com/docker/buildx/store/storeutil
#       github.com/docker/buildx/store
#       github.com/docker/docker/pkg/namesgenerator
#     compose's own tree has ZERO docker/docker imports.
#   buildx v0.36.1 and compose v5.5.0 are the CURRENT upstream releases, so there is
#   no newer version to move to, and both are prebuilt Wolfi apks we do not compile.
#   Clearing it would mean shipping our OWN from-source buildx + compose with
#   buildx's random-name generator swapped for a non-upstream one -- a behavioural
#   fork of upstream for a MEDIUM in a name generator. Not done deliberately.
#
# EVERYTHING ELSE IS FIXABLE, BUT CANNOT CLEAR THE GATE WHILE THE ABOVE STANDS, SO
# IT IS DELIBERATELY NOT APPLIED (recorded so the next pass is mechanical, not
# re-derived):
#   * golang.org/x/mod v0.38.0 -> 0.40.0 (CVE-2026-56864/-56865, HIGH) in BOTH the
#     docker-compose and docker-cli-buildx apks. Wolfi self-heal, or from-source.
#   * mc apk r21: 7x golang.org/x/net v0.54.0 (fix 0.55.0/0.56.0). Wolfi lag ONLY --
#     upstream mc RELEASE.2025-08-13T08-35-41Z pins x/net v0.42.0, so a from-source
#     build with the house float clears it (see apps/coolify-app's git-lfs step and
#     apps/coolify-realtime's cloudflared step for the pattern: build here, drop the
#     apk from apko.yaml, which also removes its var/lib/db/sbom duplicate).
#   * git-lfs apk 3.7.1-r21: 1x golang.org/x/net v0.55.0 (fix 0.56.0). Identical
#     shape; apps/coolify-app already builds git-lfs 3.7.1 from source for exactly
#     this CVE and can be copied verbatim.
#   * pack 0.38.2 (52): 51 are ordinary floors -- x/net v0.41.0, x/crypto v0.39.0,
#     x/text v0.26.0, x/mod v0.25.0, go-git/v5 v5.16.2 -> 5.19.2, go-billy/v5
#     v5.6.2 -> 5.9.0, buildkit v0.22.0 -> 0.31.1, moby/go-archive v0.1.0 -> 0.3.0,
#     circl v1.6.1 -> 1.6.3, selinux v1.12.0 -> 1.13.0, otel v1.36.0 -> 1.41.0,
#     docker/cli v28.3.0 -> 29.2.0.
#     The 52nd is docker/docker CVE-2026-33997 -- and in pack it IS removable
#     without a fork: pack's docker/docker comes from ONE file,
#     pkg/client/process_volumes.go (`//go:build linux || windows`), which uses
#     docker/docker/volume/mounts as the -v mount-spec parser. Upstream pack ships
#     its OWN alternative implementation of the same function in
#     pkg/client/process_volumes_unix.go (`//go:build unix && !linux`) built on
#     github.com/docker/cli/cli/compose/loader, with no docker/docker. Flipping the
#     two build tags (linux||windows -> windows, unix && !linux -> unix) makes the
#     linux binary use upstream's own docker/docker-free code path -- no vendoring,
#     no hand-written parser. Verified by grep: nothing else in pack (incl. main)
#     imports docker/docker outside internal/build/testdata/.
#   * railpack 0.23.0 (33): all ordinary floors -- x/net v0.52.0, x/crypto v0.49.0,
#     x/text v0.35.0, x/mod v0.38.0, containerd/v2 v2.2.2 -> 2.3.2, buildkit v0.28.1
#     -> 0.31.1, grpc v1.79.3 -> 1.82.1, in-toto-golang v0.10.0 -> 0.11.0,
#     otel/sdk v1.42.0 -> 1.43.0. Note railpack v0.38.0 already sits at x/net 0.58.0
#     / x/crypto 0.55.0 / x/text 0.41.0 / buildkit 0.32.2 with NO docker/docker, but
#     it also moves railpack's pinned mise to 2026.8.13 -- the two MUST be bumped in
#     lockstep (see the RAILPACK <-> MISE COUPLING note in melange.yaml) and upstream
#     coolify-helper still pins railpack 0.23.0 / mise 2026.3.17.
#
# UNBLOCK CONDITION (any one of):
#   1. github.com/docker/buildx stops importing docker/docker/pkg/namesgenerator
#      (then Wolfi's next compose+buildx rebuild clears CVE-2026-33997), or
#   2. the moby 29.x daemon is published on a real, non-prerelease Go module path so
#      a float can reach 29.3.1.
# Then: rebuild for the x/mod + x/net self-heal on the four apks (or build mc and
# git-lfs from source), apply the pack build-tag flip + the pack/railpack floors
# above, and re-gate. Nothing in apps/coolify-helper/ can clear it before that
# without weakening the gate, so the image stays unpublished.
# ---------------------------------------------------------------------------
# coolify-helper: from-source via melange; version pinned in melange.yaml. No render placeholders.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(1.0.15)
render() { echo "s/__VER__/$1/g"; }