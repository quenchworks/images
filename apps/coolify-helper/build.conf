# re-verified 2026-07-09 (fresh Trivy DB): STILL BLOCKED. Isolated apko+trivy probe
#   of the Wolfi apks (newest in live index: docker-compose 5.3.1-r0, docker-cli-buildx
#   0.35.0-r3, mc r16) still reports docker/docker CVE-2026-34040 (HIGH) + CVE-2026-33997
#   (MEDIUM) [moby v28 -> v29 major, via compose+buildx] and golang.org/x/net x6
#   (HIGH/MEDIUM, fixed 0.55.0) [via mc]. All Wolfi self-heal; not in-repo fixable.
# re-gated 2026-07-08 (fresh Trivy DB, live Wolfi APKINDEX): STILL BLOCKED.
#   docker-compose has since advanced to 5.3.0/5.3.1-r0 but STILL bundles
#   github.com/docker/docker v28.5.2+incompatible (CVE-2026-34040 HIGH +
#   CVE-2026-33997 MEDIUM, fix 29.3.1 = moby v29 MAJOR) — not yet rebuilt on
#   moby v29. docker-cli-buildx (0.35.0-r2) and mc (r15) unchanged: same
#   moby-v29 + x/net-0.55.0 blockers. All are Wolfi self-heal; unfixable in-repo.
# STATUS: BLOCKED (do not publish)
# ---------------------------------------------------------------------------
# The 0-CVE Trivy gate (--ignore-unfixed) FAILS on Wolfi-apk tools that carry
# fixed-upstream CVEs for which Wolfi has NOT yet published a patched apk. These
# are apks pinned/pulled in apko.yaml, NOT built from source here, so they can
# only be fixed by Wolfi rebuilding the package (Wolfi self-heal) -- nothing in
# apps/coolify-helper/ can clear them. Live Wolfi APKINDEX (verified 2026-07-04)
# still resolves to the exact vulnerable revisions below; no newer -rN exists.
#
# HARD BLOCKERS (Wolfi self-heal + a MAJOR bump we could never do in-repo):
#   * docker-compose   (Wolfi apk docker-compose-5.2.0-r1, latest in index)
#       CVE-2026-34040 (HIGH) + CVE-2026-33997 (MEDIUM)
#       github.com/docker/docker v28.5.2+incompatible -> fixed 29.3.1
#       == docker/docker v28 -> moby/moby v29 (MAJOR). Blocked until Wolfi
#          rebuilds docker-compose against the moby v29 line.
#   * docker-cli-buildx (Wolfi apk docker-cli-buildx-0.35.0-r2, latest in index)
#       CVE-2026-34040 (HIGH) + CVE-2026-33997 (MEDIUM)
#       github.com/docker/docker v28.5.2+incompatible -> fixed 29.3.1 (moby v29 MAJOR).
#          Blocked until Wolfi rebuilds docker-cli-buildx against moby v29.
#
# WOLFI SELF-HEAL BLOCKER (minor bump, but still only Wolfi can ship it):
#   * mc  (Wolfi apk mc-0.20250813.083541-r15, latest in index)
#       6x golang.org/x/net v0.54.0 -> fixed 0.55.0:
#       CVE-2026-25681, -27136, -39821, -42502 (HIGH), -25680, -42506 (MEDIUM).
#          Blocked until Wolfi rebuilds mc against golang.org/x/net 0.55.0.
#
# FIXABLE-BUT-MOOT (from-source Go tools; go-get bumps would clear THEIR rows but
# CANNOT clear the gate while the Wolfi apks above still fail -- so not touched):
#   * pack     (usr/local/bin/pack)     44 fixable CVEs (x/net, x/crypto, aws-sdk-go-v2, docker/docker, containerd, ...)
#   * railpack (usr/local/bin/railpack) 28 fixable CVEs (x/net, x/crypto, docker/docker, containerd, ...)
#   docker-cli 0, git-lfs 0, nixpacks/mise (Rust) 0 fixable.
#
# UNBLOCK CONDITION: re-run the gate once Wolfi publishes patched docker-compose,
# docker-cli-buildx (moby v29 line) and mc (x/net 0.55.0) apks; then apply the
# pack/railpack go-get bumps and rebuild to verify 0 CVEs.
# ---------------------------------------------------------------------------
# coolify-helper: from-source via melange; version pinned in melange.yaml. No render placeholders.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(1.0.14)
render() { echo "s/__VER__/$1/g"; }