# STATUS 2026-07-27: STILL BLOCKED (Wolfi lag). Both nightly findings are LOW, are
# the SAME CVE (CVE-2026-10532) reported twice, and share one root cause -- a
# vulnerable logback shaded INTO Wolfi's apk:
#  1. the `sonar-scanner-cli` apk row: Trivy marks it fixed in 8.1.0.6389-r3, but
#     Wolfi still publishes only -r0/-r1 (verified against the x86_64 + aarch64
#     APKINDEX on 2026-07-27; -r1 is the newest, vulnerable rev).
#  2. the ch.qos.logback:logback-core 1.5.33 -> 1.5.34 row. 1.5.34+ IS on Maven, but
#     logback is SHADED into the single fat jar the apk ships
#     (usr/bin/sonarqube/sonarscanner-cli.jar -- the apk's own SBOM reports
#     logback-core@1.5.33 / logback-classic@1.5.33 from inside it). There is no
#     discrete jar to swap, so the SHA256-pinned content-swap pattern used by
#     apps/graylog (lz4) and apps/keycloak (netty) does not apply here; this recipe
#     is apko-only (FROM_SOURCE=0) with no melange stage to swap in.
# No in-repo fix for either. The weekly recheck re-gates when Wolfi rebuilds the apk
# against logback 1.5.34+ (which clears both rows at once).
BLOCKED=1
# sonar-scanner-cli (SonarSource): Wolfi apk (FROM_SOURCE=0, apko-only, 0-CVE). The
# standalone scanner that pushes analysis to a SonarQube server. IMAGE-ONLY -- no
# Helm chart; you run it in CI. apko pulls the `sonar-scanner-cli` apk straight from
# Wolfi (which brings its own OpenJDK + JAVA_HOME). Tag = full version.
# The 7.3 line is dropped: it carries a fixable logback CVE (fixed only in 8.0.1.6346-r2+).
FROM_SOURCE=0
VERSIONS=(8.1.0.6389)
declare -A PIN=( [8.1.0.6389]=8.1.0.6389-r1 )
render() { echo "s|__PKG__|sonar-scanner-cli=${PIN[$1]}|g"; }
