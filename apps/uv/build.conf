# uv: apko-runtime. Wolfi ships a single floating `uv` binary package (+ a pinned
# python). We tag the full uv version Wolfi currently resolves (0.11.23); single
# version (Wolfi has one uv line). __PYVER__ = the bundled CPython minor (3.13).
FROM_SOURCE=0
VERSIONS=(0.11.23)
render() { echo "s/__PYVER__/3.13/g"; }
