# uv: uv binary on a fixed Python minor. VERSIONS = uv line (tag);
# apko only carries __PYVER__ = fixed Python 3.13 (the workflow's pyver).
FROM_SOURCE=0
VERSIONS=(0.11)
render() { echo "s/__PYVER__/3.13/g"; }
