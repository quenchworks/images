# STATUS: ZLIB-ONLY as of 2026-09-16. Measured, not assumed: run 35066644326
# on af0623d gated with exactly one finding, zlib CVE-2026-85091, on every
# version and both arches. Nothing else in this image is flagged.
#
# Wolfi is at 1.3.2.1_rc20260601-r0 and the advisory wants 1.3.3-r0, which
# upstream madler/zlib has never tagged. Downgrading to Wolfi's 1.3.1-r51
# is not a way out either: it predates the 1.3.2-r0 fix for CVE-2026-27171.
#
# BLOCKED=1 skips build+merge so sweeps stop burning a red run here. Clear
# it with scripts/blocked.py --dispatch once Wolfi ships zlib 1.3.3-r0.
# httpd (Apache HTTP Server): from-source via autoconf (./configure && make).
# Apache maintains a SINGLE stable line (2.4.x); there is no parallel 2.2/2.6, so
# we ship the NEWEST 2.4 patch only. Each release pins its source tarball by the
# sha256 published alongside it at downloads.apache.org/httpd/*.tar.bz2.sha256.
BLOCKED=1
FROM_SOURCE=1
VERSIONS=(2.4.68)
declare -A SHA256=(
  [2.4.68]=68c74d4df38c26bed4dfbdb8f3baf1eb532f3872357becc1bba5d136f6b63c06
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
