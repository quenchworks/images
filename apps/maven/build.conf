# maven: maven-__MVNVER__ layered on a fixed JDK. VERSIONS = Maven line (tag);
# __MVNVER__ = same line, __JDKVER__ = fixed JDK 21 (the workflow's jdkver).
FROM_SOURCE=0
VERSIONS=(3.9)
render() { echo "s/__VER__/$1/g; s/__JDKVER__/21/g"; }