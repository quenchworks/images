# STATUS: BLOCKED (do not publish) -- 0-CVE Trivy gate FAILS on 9.4.3 (48 fixable
# findings) and CANNOT be cleared without forking/rebuilding Elastic's own jars.
# Newest-only already applied (9.4.3 is the newest ES release; 9.4.4/9.5.0 = HTTP 404
# on artifacts.elastic.co, verified 2026-07-04). Blocking findings:
#
#  * DECISIVE (core jar, not removable, not drop-in replaceable):
#    lib/elasticsearch-x-content-9.4.3.jar  -- Elastic's OWN core server jar; it SHADES
#      jackson-core 2.17.2  -> GHSA-72hv-8253-57qq (fix 2.18.6/2.21.1). Fixing the shaded
#      jackson inside an Elastic uber-jar requires rebuilding it upstream = a FORK.
#    bin/elasticsearch-sql-cli-9.4.3.jar    -- SHADES jackson-core 2.15.0 -> same GHSA.
#    modules/{esql-datasource-iceberg,esql-datasource-parquet}/parquet-hadoop-bundle-1.17.0.jar
#      -- uber-jar SHADES jackson-databind 2.19.2 + jackson-core 2.19.2
#      -> CVE-2026-54512/54513/54514/54515 (fix 2.18.9/2.21.5). Not drop-in replaceable.
#
#  * standalone module jars (would ALSO need a distribution-wide jackson 2.15 -> 2.18.9+
#    multi-minor bump that also touches the shaded copies above):
#    modules/{ingest-geoip,repository-azure,repository-gcs,legacy-geo,x-pack-ent-search,
#             x-pack-esql,x-pack-inference,x-pack-monitoring,x-pack-sql}/
#      jackson-databind-2.15.0.jar -> CVE-2026-54512/54513/54514/54515 (fix 2.18.9/2.21.5)
#      jackson-core-2.15.0.jar     -> GHSA-72hv-8253-57qq (fix 2.18.6/2.21.1)
#    modules/{esql-datasource-azure,repository-azure,x-pack-inference}/
#      reactor-netty-http-1.0.45.jar -> CVE-2025-22227 (fix 1.2.8)
#    modules/{esql-datasource-gcs,repository-gcs}/
#      opentelemetry-api-1.47.0.jar  -> CVE-2026-45292 (fix 1.62.0)
#    modules/x-pack-ent-search/commons-lang3-3.9.jar -> CVE-2025-48924 (fix 3.18.0)
#
# WHY BLOCKED (per gate policy): the fixable jackson CVEs are SHADED inside Elastic's own
# core uber-jar (lib/elasticsearch-x-content) and the parquet-hadoop-bundle -- neither is a
# removable module nor a drop-in-replaceable standalone jar, so clearing them requires a
# fork/rebuild of Elastic's jars. No newer ES release exists to inherit the fix (9.4.3 is
# newest), and the jackson CVEs (CVE-2026-54512..515, Jul-2026) are not yet bundled by any
# ES build. Not a license block (SSPL/ELv2 caution tier is still shippable) -- a CVE block.
# Re-evaluate when Elastic ships a release bundling jackson >= 2.18.9 / 2.21.5.
#
# elasticsearch: from-source via melange (fetch the per-arch binary dist, sha512).
# Newest stable release only (full X.Y.Z, used as-is). Per-arch sha512. ES 9.x
# runs on the full openjdk-21 (needs jdk.attach), pinned in melange.yaml.
#   __VER__ = version  __SHA_AMD__ = x86_64 sha512  __SHA_ARM__ = aarch64 sha512
FROM_SOURCE=1
VERSIONS=(9.4.3)
declare -A SHA_AMD=(
  [9.4.3]=d53f8d3efeb830b73704873d082b940158deb9387937ed817eea89d2bf542ece24dae877548e4b48b0e134d5123704f04c86be23dcd9cb732a2489868a8dd0c5
)
declare -A SHA_ARM=(
  [9.4.3]=c41f4d33c7ce8fdd0754017755d05a9ba4d6326b4099525f1939cdc12a5c5e7afd1879ee45d56a90c665bdb877da4b553fda13796a60030e214ae1bd2da6fbaa
)
render() { echo "s/__VER__/$1/g; s|__SHA_AMD__|${SHA_AMD[$1]}|g; s|__SHA_ARM__|${SHA_ARM[$1]}|g"; }
