# ruby: apko-runtime from Wolfi's ruby-<minor> packages. Published tag = FULL
# version Wolfi resolves (latest 3 lines); render maps __VER__ -> the minor.
FROM_SOURCE=0
VERSIONS=(3.2.11 3.3.11 3.4.9)
render() { local v="$1"; echo "s/__VER__/${v%.*}/g"; }
