# STATUS 2026-08-27 (later the same day): UNBLOCKED. BLOCKED=1 -> 0.
#   what the previous note claimed (correctly, as far as it went): everything
#        except git-lfs was already clean after the VEX refresh, and the ONE
#        remaining finding was CVE-2026-46600 (HIGH, DoS in
#        golang.org/x/net/dns/dnsmessage; x/net v0.55.0, fixed in 0.56.0) against
#        /usr/bin/git-lfs, reported TWICE -- once against the binary and once
#        against its bundled var/lib/db/sbom/git-lfs-3.7.1-r21.spdx.json. It also
#        stated the fix path (shadow the apk) and then declined to take it,
#        because that needed a melange step in
#        .github/workflows/build-jenkins-inbound-agent.yml, outside this app's
#        directory. That was the only thing standing in the way. It has been done.
#   re-verified before fixing, all still true today: Wolfi's newest git-lfs is
#        3.7.1-r21 (live x86_64 APKINDEX; r17..r21 are the last five revisions),
#        built against x/net v0.55.0, with no fixed revision in Wolfi's secdb;
#        and jenkins-utils 2.566-r1's APKINDEX line is
#        `D: bash bash-binsh coreutils curl debianutils diffutils findutils git
#        git-lfs glibc-locale-en ...` -- an UNPINNED HARD dependency, so unlike
#        apps/argocd and apps/coolify-app we cannot drop the apk from apko.yaml.
#   the fix -- a SHADOW APK. New apps/jenkins-inbound-agent/melange.yaml builds a
#        package NAMED git-lfs at version 3.7.1 epoch 22 (i.e. 3.7.1-r22, one
#        revision above Wolfi's newest) from upstream source at tag v3.7.1,
#        commit b84b33847fe6458f36ef521534dc0eac953cb379 asserted, with the
#        golang.org/x/* family floated coherently
#        (`go mod edit -require=golang.org/x/net@v0.58.0
#        -require=golang.org/x/text@v0.41.0`, then an in-pipeline assert that the
#        FLOOR x/net >= v0.56.0 and x/text >= v0.39.0 reached the SHIPPED BINARY).
#        Copied from the equivalent step in apps/coolify-app/melange.yaml rather
#        than invented. apko.yaml gained the `./melange.rsa.pub` keyring entry, the
#        `@local ./packages` repository and `- git-lfs@local`; the workflow gained
#        melange install + render + build steps (it was apko-only) before
#        `apko build`.
#   RESOLUTION PROVED, not assumed. First attempt used a BARE `- git-lfs`, betting
#        that r22 > r21 would win on version alone. It did NOT: apko installed
#        `git-lfs (3.7.1-r21)`, Wolfi's. apko's resolver needs the repository pin,
#        so `git-lfs@local` is REQUIRED and the higher epoch is necessary but not
#        sufficient. With the pin, the built image's usr/lib/apk/db/installed reads
#        `P:git-lfs / V:3.7.1-r22 / o:git-lfs / m:unknown` -- ours -- on BOTH
#        arches, and because it carries the same NAME it also satisfies
#        jenkins-utils's bare `git-lfs` dep, so Wolfi's r21 is never fetched and
#        there is no file conflict over /usr/bin/git-lfs.
#        `go version -m usr/bin/git-lfs` extracted from the image layer reports
#        `golang.org/x/net v0.58.0` and `golang.org/x/text v0.41.0` on x86_64 AND
#        aarch64 (x/crypto stays at v0.55.0, which Trivy does not flag).
#   the SECOND report of the CVE (the bundled SBOM) is gone too, and NOT by
#        suppression. The vulnerable row came from the Wolfi apk's own
#        var/lib/db/sbom/git-lfs-3.7.1-r21.spdx.json, which enumerates its Go
#        module purls. That file is not in the image any more -- our package ships
#        var/lib/db/sbom/git-lfs-3.7.1-r22.spdx.json instead, melange-generated,
#        whose only entries are the apk itself
#        (pkg:apk/unknown/git-lfs@3.7.1-r22?arch=x86_64) and the recipe. It makes
#        no Go module claim at all, so nothing stale can be inherited from it and
#        Trivy falls back to the embedded build info of the real binary -- which is
#        the authoritative module list and is clean. Checked by hand after the
#        build; there is exactly one git-lfs SBOM in the layer and it describes
#        what ships.
#   vex.openvex.json: UNTOUCHED. Still 25 statements, still the origin-inherited
#        o:jenkins-2 false-positive class only. CVE-2026-46600 was real and is
#        fixed in code, not argued away. `scripts/check-vex.py` passes on every run
#        ("ok jenkins-inbound-agent: 25 statement(s)").
#   final gate, verbatim, IDENTICAL on all three matrix legs (2.564-r1, 2.565-r0,
#        2.566-r1) on x86_64 and on aarch64:
#          image.tar (wolfi 20230201)                    wolfi     0
#          Java                                          jar       0
#          usr/share/jenkins/remoting-3355.v...jar        jar       0
#          usr/bin/git-lfs                               gobinary  0
#        -> "✅ 0 fixable CVEs". comprehensive mode, CHML, --ignore-unfixed, no
#        .trivyignore, no --skip-*, no severity change. The
#        var/lib/db/sbom/git-lfs-*.spdx.json target that used to appear as a fifth
#        row is simply absent now.
#   boot test: PASSES on the x86_64 2.566 build -- jenkins-agent entrypoint,
#        working JRE, agent.jar present, nonroot 1001. Plus
#        `git-lfs version` in the image prints
#        "git-lfs/3.7.1 (GitHub; linux amd64; go 1.27.0; git v3.7.1)", so the
#        shadow binary is a working git-lfs and not just a clean one.
#   NOT self-expiring -- needs a human. When Wolfi publishes a git-lfs built
#        against x/net >= 0.56.0 (3.7.1-r23 or later), the `@local` pin keeps
#        using ours regardless. At that point: delete melange.yaml, remove
#        `- git-lfs@local` + the `@local ./packages` repo + the `./melange.rsa.pub`
#        keyring entry from apko.yaml, drop the melange steps from the workflow,
#        and set FROM_SOURCE back to 0.
#   published state -- STILL STALE, unchanged by this pass (PUSH=0 throughout).
#        GHCR hosts 2.542 / 2.543 / 2.544 created 2026-07-19 from the RETIRED flat
#        jenkins-docker-agent apk; a hand scan of 2.544 on 2026-08-27 reported 94
#        fixable rows including CVE-2026-2332 (CRITICAL). catalog.lock.yaml still
#        carries `versions: []` for this app, so the nightly published-image scan
#        skips it -- its absence from that report means "not scanned", NOT "clean".
#        Treat the published tags as superseded; this app now needs a real
#        publishing run.
#   one thing found but NOT fixed (outside this app's directory):
#        scripts/build-image.sh calls `uv run "$(dirname "$0")/check-vex.py"` AFTER
#        cd-ing into apps/<app>, so invoking it as `bash scripts/build-image.sh`
#        from the repo root makes that a RELATIVE path that does not exist and
#        every VEX-carrying app dies with "Failed to spawn: scripts/check-vex.py"
#        before the scan. Invoking it by ABSOLUTE path works; all gate runs above
#        were done that way. The fix is one `$ROOT/scripts/check-vex.py`.
#
# STALE CLAIM DISPROVED (2026-09-14): the note directly above says "published
#   state STILL STALE... catalog.lock.yaml still carries `versions: []`". That
#   is no longer true and, per catalog.lock.yaml's own `published:` timestamps
#   (2026-08-27T08:56:3x/41Z for all three versions), was already false within
#   hours of being written -- a publish run DID happen later that same day, it
#   just never got recorded here. All three versions ARE published and ARE
#   being nightly-scanned; the "not scanned" excuse for this app no longer
#   applies to anything.
#
# STATUS 2026-09-14: RE-BLOCKED. BLOCKED=0 -> 1, for TWO independent reasons,
#   neither of which is stale-image drift (re-verified against today's live
#   Wolfi APKINDEX, both arches, and today's nightly scan of the actual
#   published 08-27 digests, which agree):
#
#   1. NEW Jenkins-core CVEs, Wolfi hasn't caught up. CVE-2026-54512/54513
#      (HIGH), 54514/54516/54517/54518 (MEDIUM, all fixed jenkins 2.572-r0),
#      54515 (MEDIUM, fixed 2.574-r0), 2025-48924 + CVE-2026-47065 (LOW, fixed
#      2.577-r0/2.579-r0) hit jenkins-docker-agent-openjdk-21, jenkins-remoting
#      AND jenkins-utils at 2.566-r1 -- disclosed after the 08-27 gate (that
#      gate's own table showed these three packages at 0 findings). Checked
#      Wolfi's live APKINDEX today, x86_64 AND aarch64: newest of ALL FOUR
#      jenkins-2/docker-agent/remoting/utils packages is STILL 2.566-r1 (also
#      confirmed against openjdk-25 and openjdk-17 variants -- none are ahead).
#      2.572/2.574/2.577/2.579 do not exist anywhere in Wolfi yet. This is a
#      genuine, current Wolfi-secdb lag on the vanilla apk assembly half of
#      this recipe (not FROM_SOURCE=1 territory) -- nothing in this app's
#      directory can fix it; it self-resolves the day Wolfi rebuilds.
#   2. git-lfs shadow needed a floor bump. CVE-2026-56855 + CVE-2026-78662
#      (MEDIUM, golang.org/x/crypto, fixed 0.56.0) now hit our shadow git-lfs
#      3.7.1-r22 -- x/crypto was v0.55.0, fine on 08-27 (only x/net was in
#      scope then), not fine now. FIXED here: melange.yaml's coherent float
#      gained `-require=golang.org/x/crypto@v0.57.0` plus a matching assert
#      row. Before floating further, checked whether Wolfi's git-lfs had
#      finally caught up and the shadow could be dropped per the "not
#      self-expiring" note below -- it has NOT: Wolfi published a real new
#      release, git-lfs 3.8.0-r1 (both arches), but `go version -m` on the
#      downloaded apk shows it still ships golang.org/x/crypto v0.54.0, WORSE
#      than our own v0.55.0, so unshadowing now would reintroduce
#      CVE-2026-56854 too (HIGH, fixed 0.55.0). Shadow stays; apko.yaml comment
#      updated to record the re-check.
#
#   Net effect once rebuilt: reason 2 is fully fixed by this pass (git-lfs
#   clears). Reason 1 is NOT fixable from this directory and keeps the gate
#   red until Wolfi ships jenkins-* >= 2.572-r0 on all three affected packages.
#   glibc-2.44/libcrypt1/ld-linux (r1->r6), linux-pam (r4->r5) and curl/
#   libcurl-openssl4 (8.21.0-r2->8.22.0-r2) are unpinned transitive deps and
#   need no edit -- a rebuild alone picks up Wolfi's current builds (verified
#   present on both arches). zlib (1.3.2-r4, needs 1.3.3-r0) is the standing
#   KNOWN WALL -- absent from Wolfi on both arches, not fought here.
# STATUS 2026-09-16: STILL BLOCKED, but the 09-14 REASON IS WRONG. Re-measured
#   end to end today with a real local gate run (PUSH=0 ARCHES=x86_64,
#   version 2.566, comprehensive mode, CHML, --ignore-unfixed, the app's own
#   untouched 25-statement VEX). Verbatim result:
#     image.tar (wolfi 20230201)                     wolfi     1
#     Java                                           jar       0
#     usr/share/jenkins/remoting-3355.v...jar        jar       0
#     usr/bin/git-lfs                                gobinary  0
#     -> the ONE row is zlib 1.3.2.1_rc20260601-r0, CVE-2026-85091 (MEDIUM),
#        fixed 1.3.3-r0.
#
#   REASON 1 OF THE 09-14 BLOCK ("Jenkins-core CVEs Wolfi has not rebuilt yet")
#   DOES NOT BLOCK THIS GATE AND NEVER DID. Every CVE it names was ALREADY in
#   vex.openvex.json before 09-14 (git log: the file is untouched since
#   1b98017), as the origin-inherited o:jenkins-2 false-positive class:
#     CVE-2026-54512, 54513 (HIGH)  -> Wolfi fix 2.572-r0
#     CVE-2026-54514, 54516, 54517, 54518 (MEDIUM) -> 2.572-r0
#     CVE-2026-54515 (MEDIUM)       -> 2.574-r0
#     CVE-2026-47065 (LOW)          -> 2.577-r0
#     CVE-2025-48924 (LOW)          -> 2.579-r0
#   Proved by scanning TODAY's image.tar twice, same tar, VEX on and VEX off:
#   without --vex exactly those 9 CVEs appear against
#   jenkins-docker-agent-openjdk-21 / jenkins-remoting / jenkins-utils, all at
#   2.566-r1; with --vex all three packages are 0. So the 09-14 observation was
#   a NO-VEX scan (the nightly published-image scan does not pass TRIVY_VEX),
#   not a new gate failure. The CI workflow does pass it (TRIVY_VEX=..., fixed
#   in a1edf81), so CI would have cleared them too.
#   Wolfi side re-measured today anyway, live APKINDEX, BOTH arches: jenkins-2,
#   jenkins-2-openjdk-21, jenkins-docker-agent-openjdk-21, jenkins-remoting and
#   jenkins-utils are ALL still capped at 2.566-r1 -- 2.572/2.574/2.577/2.579
#   exist nowhere in the os repo. That confirms the VEX's own secondary
#   argument (those fixed-versions are published under a different origin, not
#   o:jenkins-2) rather than contradicting it. None of the 9 is a "0" bucket in
#   security.json, so none is a permanent never-fix -- but none needs to be
#   waited on either, because the VEX already clears them on evidence.
#
#   REASON 2 OF THE 09-14 BLOCK (git-lfs x/crypto) IS FIXED AND VERIFIED. The
#   shadow git-lfs 3.7.1-r22 built clean today with the
#   x/net@v0.58.0 + x/text@v0.41.0 + x/crypto@v0.57.0 float and its in-pipeline
#   floor assert; usr/bin/git-lfs scans 0. Wolfi's own git-lfs is 3.8.0-r1 on
#   both arches and still ships x/crypto v0.54.0, so the shadow still cannot be
#   dropped -- unchanged from the 09-14 re-check.
#
#   WHAT ACTUALLY BLOCKS: zlib CVE-2026-85091 ONLY -- the standing known wall,
#   and it is a WAITING GAME, not permanent. security.json keys the fix to
#   zlib 1.3.3-r0. Wolfi has MOVED since 09-14 (1.3.2-r4 -> 1.3.2.1_rc20260601-r0,
#   identical on x86_64 and aarch64) but apk-compares BELOW 1.3.3, so Trivy
#   still reports it. zlib's only "0" bucket is CVE-2026-22184, a different and
#   unfixed advisory that --ignore-unfixed drops; CVE-2026-85091 is NOT a "0"
#   bucket. zlib arrives transitively via git/curl/openjdk and cannot be
#   dropped from the closure. Nothing in this app's directory can fix it.
#   UNBLOCK CONDITION: Wolfi publishes zlib 1.3.3-r0 (or any version that
#   apk-compares >= it). Then clear BLOCKED and rebuild -- no recipe change is
#   expected, everything else in this image is already at 0.
#   Not re-run on aarch64: the gate outcome is decided by zlib, whose version is
#   byte-identical on both arches in today's index, and PUSH=0 throughout.
# STATUS 2026-09-16: measured on run 35121940698 (commit 742b2a0). All SIX build
# legs, 2.564 / 2.565 / 2.566 across both arches, fail on one finding: zlib
# CVE-2026-85091. Nothing else is flagged; the app's VEX clears the nine
# Jenkins-core CVEs an earlier no-VEX nightly scan had reported.
#
# This replaces a reading taken from run 34020172913 of 2026-09-06, where 5 of 6
# legs passed and only 2.566 aarch64 failed. That result does not carry forward:
# four recipe commits landed since, including the VEX-in-CI fix. Today every leg
# is walled, so there is no publishable line to rescue the way postgrest had.
#
# UNBLOCKED 2026-09-20. "Every leg is walled" was a statement about zlib
# CVE-2026-85091, and that wall is gone: Wolfi shipped the fix as
# 1.3.2.1_rc20260601-r0 and the advisory was amended to name it. The 2026-09-06
# reading above, where only the 2.566 aarch64 leg failed, is left in place as
# history rather than treated as current.
# Re-gated locally 2026-09-20 at PUSH=0 on x86_64: 0 fixable CVEs AND the boot
# test passed. aarch64 is left to CI's native runner, which refuses to publish
# on any finding.
BLOCKED=0
#
# jenkins-inbound-agent: the Jenkins inbound (JNLP/websocket) build agent -- the
# jenkins-agent launch script + the remoting agent.jar + an OpenJDK. IMAGE-ONLY --
# no Helm chart; the Jenkins controller launches it. apko pulls
# `jenkins-docker-agent-openjdk-21` (the agent script + its OpenJDK 21 dep, which
# also drags in jenkins-utils) and `jenkins-remoting` (agent.jar) straight from
# Wolfi. Latest 3 docker-agent lines; tag = the docker-agent version.
#
# FROM_SOURCE=1 as of 2026-08-27, and it means LESS than usual: the Jenkins bits
# are still pure Wolfi apk assembly and version discovery is unchanged
# (scripts/check/jenkins-inbound-agent.py reads the Wolfi apk index). The single
# melange package is the SHADOW git-lfs described in the STATUS note above --
# jenkins-utils drags an unpinned, vulnerable git-lfs in, so we ship our own at a
# higher revision. Same instrument apps/poetry uses for its pip-vendor shim.
#
# PACKAGE RENAME 2026-07-27 (still true 2026-08-27): Wolfi RETIRED the flat
# `jenkins-docker-agent` apk -- it is absent from the APKINDEX at ANY version, so
# the old `jenkins-docker-agent=2.544-r0` pin is unresolvable. It was split per
# JDK. Only -openjdk-21 and -openjdk-25 exist now; the -17 variant has since
# disappeared from the index entirely (jenkins-2-openjdk-17 is frozen at
# 2.544-r1). We track -openjdk-21 (LTS). The shipped /usr/local/bin/jenkins-agent
# script is byte-for-byte the same upstream launcher (same /usr/share/jenkins/
# agent.jar path, same bash dep), so test.sh is unchanged. No openjdk-21 CVEs.
FROM_SOURCE=1
VERSIONS=(2.564 2.565 2.566)
# test.sh takes its extra args from build-jenkins-inbound-agent.yml, which passes "" rather
# than the version. Mirrored here so the local gate matches CI; passing the
# version instead fails an image that runs fine.
boot_args() { echo ""; }
declare -A PIN=( [2.564]=2.564-r1 [2.565]=2.565-r0 [2.566]=2.566-r1 )
# melange.yaml carries no placeholders (the shadow git-lfs is pinned at 3.7.1
# independently of the agent version); only apko.yaml's __PKG__ is substituted.
render() { echo "s|__PKG__|jenkins-docker-agent-openjdk-21=${PIN[$1]}|g"; }
