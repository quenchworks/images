# STATUS 2026-08-27: STILL BLOCKED, but now by exactly ONE finding, in a
# dependency we do not build. Rebuilt, re-triaged, VEX refreshed.
#   what the old notes claimed: the 2026-07-27 note said the 2.544-r0 ->
#        2.566-r1 rename/bump clears 34 of 57 nightly rows and leaves "21 rows
#        (6 HIGH + 15 MEDIUM)" of the origin-inherited false-positive class,
#        plus git-lfs x/net + x/crypto as Wolfi-gated, and told the maintainer to
#        extend vex.openvex.json before release. The 2026-07-13 note above it
#        claimed the gate "now PASSES" -- that half was already false the moment
#        Wolfi retired the flat apk the VEX purls named.
#   what the rebuild actually showed (x86_64, PUSH=0, apko-only, 2.566 leg):
#        29 fixable rows with NO vex at all, 17 with the stale vex. Not 21, and
#        the split was different: 27 rows are the o:jenkins-2 class (9 distinct
#        CVEs x 3 subpackages) and 2 rows are ONE git-lfs CVE. The x/crypto and
#        x/text git-lfs rows the old note listed are gone on their own
#        (3.7.1-r21 ships x/crypto v0.52.0 + x/text v0.39.0, neither flagged).
#   class 1, FIXED -- 27 rows, origin-inherited false-positive.
#        CVE-2026-54512/54513 (HIGH), CVE-2026-54514/54515/54516/54517/54518
#        (MEDIUM), CVE-2025-48924 + CVE-2026-47065 (LOW), each reported three
#        times: once against jenkins-docker-agent-openjdk-21, once against
#        jenkins-remoting, once against jenkins-utils, all installed 2.566-r1.
#        Wolfi keys these advisories to the package ORIGIN `jenkins-2`
#        (confirmed in https://packages.wolfi.dev/os/security.json, where the
#        secfixes live under the name `jenkins-2` and nowhere else) and Trivy
#        inherits them onto every subpackage. The libraries named -- jackson-
#        databind, Apache MINA, commons-lang3 -- live in the CONTROLLER
#        (jenkins.war), not in an agent. Re-verified 2026-08-27 by extracting
#        the built layer: the image ships four jars in total
#        (remoting-3355.v388858a_47b_33.jar plus its agent.jar/slave.jar
#        symlinks, and the JDK's jrt-fs.jar); `unzip -l` on both real jars and a
#        raw byte grep over every other file in the layer return 0 matches for
#        com/fasterxml/jackson, org/apache/mina, org/apache/sshd,
#        org/apache/commons/lang3 and org/springframework. Secondary proof that
#        the advisory is mis-keyed: its fixed-versions (2.572-r0 / 2.574-r0 /
#        2.577-r0 / 2.579-r0) do not exist for origin jenkins-2 at all, which
#        the live APKINDEX caps at 2.566-r1; 2.569..2.579 are published only
#        under the separate o:jenkins-docker origin (jenkins-docker,
#        jenkins-entrypoint) -- the controller image packages.
#        Fix applied: vex.openvex.json bumped to version 2 / 2026-08-27. The
#        products of every statement now include
#        pkg:apk/wolfi/jenkins-docker-agent-openjdk-21 (the rename the old file
#        never got; that is why 12 of these rows were not being suppressed even
#        though 6 of the CVE IDs were already listed), and three statements were
#        added for the CVEs the file did not cover at all: CVE-2026-54515,
#        CVE-2025-48924, CVE-2026-47065. The retired
#        pkg:apk/wolfi/jenkins-docker-agent purl and the 13 pre-2.566 CVE
#        statements are KEPT on purpose: CI gates all three matrix legs, and the
#        2.564-r1 / 2.565-r0 legs still carry the older advisories. 25
#        statements, all not_affected / vulnerable_code_not_present, no
#        .trivyignore and no --skip-*. Verified: after the refresh the wolfi
#        target reports 0 on ALL THREE matrix legs (2.564-r1, 2.565-r0,
#        2.566-r1), so the retained pre-2.566 statements are doing their job.
#   class 2, NOT FIXABLE HERE -- 2 rows, one CVE, and the reason this stays
#        BLOCKED=1. CVE-2026-46600 (HIGH, DoS in golang.org/x/net/dns/dnsmessage),
#        golang.org/x/net v0.55.0 -> 0.56.0, reported against /usr/bin/git-lfs
#        and again against its bundled var/lib/db/sbom/git-lfs-3.7.1-r21.spdx.json.
#        git-lfs is NOT ours to pin: jenkins-docker-agent-openjdk-21 hard-depends
#        on jenkins-utils, which hard-depends on git-lfs unpinned, so apko takes
#        Wolfi's newest -- and Wolfi's newest IS 3.7.1-r21 (verified against the
#        live x86_64 APKINDEX on 2026-08-27; r17..r21 are the last five
#        revisions). `go version -m` on the shipped binary records
#        golang.org/x/net v0.55.0, so r21 is genuinely vulnerable, and Wolfi's
#        secdb carries no fixed revision for it (the git-lfs secfixes entry is
#        keyed to "0"). There is nothing to rebuild onto. Not the only image
#        affected: tonight's published scan reports the same CVE against
#        ingress-nginx and mimir.
#   unblock condition, either one:
#        (a) Wolfi publishes git-lfs >= 3.7.1-r22 built against x/net >= 0.56.0.
#            Then a PLAIN REBUILD passes -- nothing in this recipe pins git-lfs.
#        (b) shadow the apk, the way apps/argocd and apps/coolify-app already
#            handle this exact CVE: build git-lfs v3.7.1 from source with the
#            golang.org/x/* family floated (docs/go-module-float.md). Those two
#            simply omit the apk, which is not available to us because
#            jenkins-utils drags it in; here it means a melange.yaml shipping a
#            package NAMED git-lfs at 3.7.1-r22 so apk resolves jenkins-utils's
#            dependency to ours, plus a melange step in
#            .github/workflows/build-jenkins-inbound-agent.yml, which is apko-only
#            today. That workflow change is outside this app's directory and was
#            not made.
#   final gate, x86_64, all three legs identical: everything except git-lfs is
#        clean --
#          image.tar (wolfi 20230201)                    wolfi     0
#          Java                                          jar       0
#          usr/share/jenkins/remoting-3355.v...jar        jar       0
#          usr/bin/git-lfs                               gobinary  1
#          var/lib/db/sbom/git-lfs-3.7.1-r21.spdx.json   gobinary  1
#        i.e. 17 fixable rows before this pass, 2 after, and the 2 are one CVE
#        counted twice (binary + the apk's bundled SBOM). Gate exit 1.
#   what the image is otherwise: fine. test.sh passes on the 2.566 x86_64 build
#        (jenkins-agent entrypoint, working JRE, agent.jar present, nonroot
#        1001). The 2.564-r1 and 2.565-r0 legs assemble too. aarch64 was NOT
#        built this pass -- no reason to expect a difference (same unpinned
#        git-lfs), but it is unverified.
#   published state -- IT DID PUBLISH, and what is up there is badly stale.
#        GHCR hosts 2.542 / 2.543 / 2.544 (+ per-arch tags and signatures), all
#        created 2026-07-19 from the RETIRED flat jenkins-docker-agent apk.
#        catalog.lock.yaml carries `versions: []` for this app, so
#        website/src/data/images.json has no digests and the nightly
#        published-image scan skips it entirely -- its absence from that report
#        means "not scanned", NOT "clean". Scanned by hand on 2026-08-27
#        (2.544, sha256:649fe96b152cdc7e5059606e77556dd6e1523af2557ea2006689886f55d27c8c,
#        same gate flags, no vex): 94 fixable rows, including CVE-2026-2332
#        (CRITICAL) against jenkins-docker-agent 2.544-r0. Nearly all of it is
#        six weeks of drift a rebuild clears -- openjdk-17 17.0.19-r4 (the old
#        flat apk pulled JDK 17; the -openjdk-21 recipe does not), openssl
#        3.6.3-r3 -> r5, libnghttp2, git-lfs 3.7.1-r17 -> r21 and the Go stdlib
#        1.26.5 rows that come with it, plus the pre-2.566 jenkins-2 advisories.
#        The local rebuild above leaves exactly one of those 94 standing. So the
#        published tags should be treated as superseded, not as a passing build.
BLOCKED=1
#
# jenkins-inbound-agent: Wolfi apk (FROM_SOURCE=0, apko-only, 0-CVE target). The
# Jenkins inbound (JNLP/websocket) build agent: the jenkins-agent launch script +
# the remoting agent.jar + an OpenJDK. IMAGE-ONLY -- no Helm chart; the Jenkins
# controller launches it. apko pulls `jenkins-docker-agent-openjdk-21` (the agent
# script + its OpenJDK 21 dep, which also drags in jenkins-utils) and
# `jenkins-remoting` (agent.jar) straight from Wolfi.
# Latest 3 docker-agent lines; tag = the docker-agent version.
#
# PACKAGE RENAME 2026-07-27 (still true 2026-08-27): Wolfi RETIRED the flat
# `jenkins-docker-agent` apk -- it is absent from the APKINDEX at ANY version, so
# the old `jenkins-docker-agent=2.544-r0` pin is unresolvable. It was split per
# JDK. Only -openjdk-21 and -openjdk-25 exist now; the -17 variant has since
# disappeared from the index entirely (jenkins-2-openjdk-17 is frozen at
# 2.544-r1). We track -openjdk-21 (LTS). The shipped /usr/local/bin/jenkins-agent
# script is byte-for-byte the same upstream launcher (same /usr/share/jenkins/
# agent.jar path, same bash dep), so test.sh is unchanged. No openjdk-21 CVEs.
FROM_SOURCE=0
VERSIONS=(2.564 2.565 2.566)
declare -A PIN=( [2.564]=2.564-r1 [2.565]=2.565-r0 [2.566]=2.566-r1 )
render() { echo "s|__PKG__|jenkins-docker-agent-openjdk-21=${PIN[$1]}|g"; }
