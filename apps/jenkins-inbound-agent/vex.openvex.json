{
  "@context": "https://openvex.dev/ns/v0.2.0",
  "@id": "https://quench-works.com/vex/jenkins-inbound-agent/2026-07-13",
  "author": "QuenchWorks <info@quench-works.com>",
  "timestamp": "2026-07-13T00:00:00Z",
  "version": 1,
  "statements": [
    {
      "vulnerability": {
        "name": "CVE-2020-36843"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2025-7962"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-0636"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-22732"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-22746"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-22751"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-2332"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-3505"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-41409"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-41635"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-42519"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-42521"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-42778"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-42779"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54512"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54513"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54514"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54516"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54517"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-54518"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-5588"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-5598"
      },
      "products": [
        {
          "@id": "pkg:apk/wolfi/jenkins-docker-agent"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-remoting"
        },
        {
          "@id": "pkg:apk/wolfi/jenkins-utils"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_present",
      "impact_statement": "PROVEN false-positive. These advisories are Wolfi apk secdb entries keyed to the shared package origin 'o:jenkins-2' and are inherited by every jenkins-2 subpackage (jenkins-docker-agent, jenkins-remoting, jenkins-utils) from the Jenkins CONTROLLER (jenkins.war), where the vulnerable libraries actually live (jackson-databind, Apache MINA/sshd, Spring Security, and Jenkins core for CVE-2026-2332). This is the Jenkins INBOUND AGENT image, which does NOT ship the controller or any of those libraries. Verified by full-layer extraction: the image ships only remoting-*.jar (with agent.jar/slave.jar symlinks) plus the JDK's jrt-fs.jar; a grep for jackson/databind/mina/sshd/springframework across every layer returns 0 matches. The vulnerable code is not present in this image, so the reported CVEs are not exploitable here."
    }
  ]
}
