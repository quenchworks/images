# STATUS: BLOCKED (do not publish)
# BLOCKER (2026-07-04): opt/neo4j/lib/parquet-jackson-1.17.1.jar SHADES jackson-databind
#   2.21.3 -- classes are relocated under shaded/parquet/com/fasterxml/jackson/databind/
#   and the version is declared in the embedded
#   META-INF/maven/com.fasterxml.jackson.core/jackson-databind/pom.properties (version=2.21.3).
#   Trivy flags the Jul-2026 jackson-databind cluster on that shaded copy:
#   CVE-2026-54512, CVE-2026-54513, CVE-2026-54514, CVE-2026-54515, CVE-2026-54516,
#   CVE-2026-54517, CVE-2026-54518 (fixed in jackson-databind 2.21.4).
#   Because the vulnerable jackson is SHADED inside a parquet uber-jar (not a standalone
#   lib/ jar) it cannot be remediated by a same-major standalone jar swap, and 2026.05.0 is
#   the newest Neo4j release (2026.05.1 / 2026.06.0 / 2026.06.1 / 2026.07.0 all 404 on
#   dist.neo4j.org) so no newer bundle carries a fixed parquet-jackson. Rebuilding the
#   parquet uber-jar or bumping parquet wholesale is a cross-version change outside the
#   0-CVE minimal-swap policy.
#   (Standalone lib/ jars jackson-databind-2.21.2, netty-*-4.2.14.Final and shiro-*-2.1.0
#   are ALSO fixable via same-major swaps, but are moot while the shaded parquet-jackson
#   blocks the gate.)
#   REVISIT when Neo4j ships a release bundling parquet-jackson with jackson-databind >= 2.21.4.
#
# neo4j: from-source via melange (installs the Neo4j Community dist tarball, runs
# on the system openjdk-21-jre). Neo4j uses calendar versioning; latest 3 = the
# 3 newest monthly releases (newest patch each). Each pins its tarball by sha256.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(2026.03.1 2026.04.0 2026.05.0)
declare -A SHA256=(
  [2026.03.1]=419c5a471a8b6918570da687215d7d3406983a6ae209fd3d96c2de2a90a5dcfb
  [2026.04.0]=fd750466b1247c0d1ef09a84c614f7e045793b30dfa277148e8da71646598820
  [2026.05.0]=bb753b4e9bcc331e90b968edd8da445e974090867ca825cc672defdad6066f0e
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
