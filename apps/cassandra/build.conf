# STATUS: BLOCKED (do not publish)
#   BLOCKER: lib/jackson-databind-2.19.2.jar (STANDALONE jar in Cassandra's lib/,
#            NOT shaded in any uber-jar) -- CVE-2026-54515 (jackson-databind:
#            "Ignored properties can be unexpectedly modified").
#   WHY UNFIXABLE: Trivy lists fixed versions 3.1.4, 2.18.9, 2.21.5, 2.22.1. Every
#            same-major (2.x) fix is UNPUBLISHED on Maven Central (verified 404 for
#            jackson-databind 2.18.9 / 2.21.5 / 2.22.1; the published maxima are
#            2.18.8, 2.21.4, 2.22.0 -- all BELOW the fix threshold). The only
#            available fix is 3.1.4, a MAJOR bump (com.fasterxml.jackson -> jackson
#            3.x namespace/groupId change) that Cassandra 5.0.8 -- compiled against
#            jackson 2.19.2 -- cannot consume without an upstream source rebuild.
#            5.0.8 is already the newest Cassandra release, so no newer dist ships
#            a fixed jar. A standalone same-major swap therefore CANNOT clear the
#            0-CVE gate. Revisit when jackson-databind 2.21.5+ (or a Cassandra
#            5.0.9+ bundling it) is published.
#   NOTE: the OTHER flagged jars ARE same-major fixable once unblocked -- netty
#            4.1.130.Final -> 4.1.135.Final (all modules), logback 1.5.18 -> 1.5.33
#            (core+classic), jackson-core/annotations/databind/datatype-jsr310
#            2.19.2 -> 2.21.4 -- but 54515 keeps the gate red regardless.
#
# cassandra: from-source via melange (fetch the apache binary dist + sha512).
# Latest 3 stable minor lines, each at its newest patch (full X.Y.Z, used as-is).
# Cassandra's supported JDK differs by line (4.0/4.1 -> Java 11, 5.0 -> Java 17),
# so the JRE package AND its /usr/lib/jvm home are pinned PER version too.
#   __VER__     = version (apache-cassandra-__VER__-bin.tar.gz)
#   __SHA512__  = that tarball's sha512
#   __JREPKG__  = the Wolfi JRE package for this cassandra line
#   __JHOME__   = the JAVA_HOME dir name under /usr/lib/jvm
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(4.0.20 4.1.11 5.0.8)
declare -A SHA512=(
  [4.0.20]=4abd74a1a488360d94ee3bca905c7095cac2e3328577e46622aa41144b1a612844af79ba5ec3e98b26d08fee168511fac128bc6ea2eb2026f981d72bfdeb9fdb
  [4.1.11]=c31ead4e137774964818b3acfc2a16c329cce412ebb003baf447c3d453800a590d970cbe98641bd81539fd7a7fb8a1b6b569e5072c93fc2e18f40eaa638225be
  [5.0.8]=06b7af18c8f41dc13dbfdae186d565a5f91e71ea413c3c5373aec8a4e5074c6dee5632a0e5e98c21665f08f7291d7ca384f61a323c02610c6bca51e074b5a0c4
)
declare -A JRE=(
  [4.0.20]=openjdk-11-jre
  [4.1.11]=openjdk-11-jre
  [5.0.8]=openjdk-17-jre
)
declare -A JHOME=(
  [4.0.20]=java-11-openjdk
  [4.1.11]=java-11-openjdk
  [5.0.8]=java-17-openjdk
)
render() {
  echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g; s/__JREPKG__/${JRE[$1]}/g; s/__JHOME__/${JHOME[$1]}/g"
}
