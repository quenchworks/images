# STATUS: ZLIB-ONLY as of 2026-09-16. Measured, not assumed: run 35066555477
# on af0623d gated with exactly one finding, zlib CVE-2026-85091, on every
# version and both arches. Nothing else in this image is flagged.
#
# Wolfi is at 1.3.2.1_rc20260601-r0 and the advisory wants 1.3.3-r0, which
# upstream madler/zlib has never tagged. Downgrading to Wolfi's 1.3.1-r51
# is not a way out either: it predates the 1.3.2-r0 fix for CVE-2026-27171.
#
# BLOCKED=1 skips build+merge so sweeps stop burning a red run here. Clear
# it with scripts/blocked.py --dispatch once Wolfi ships zlib 1.3.3-r0.
# elixir: Wolfi packages elixir as erlNN-elixir-X.Y which PROVIDES elixir-<minor>;
# apko resolves the bare `elixir-<minor>` virtual (and pulls erlang). Latest 3
# minor lines, full X.Y.Z (as-is tag); PKG maps each to its elixir-<minor> virtual.
BLOCKED=1
FROM_SOURCE=0
VERSIONS=(1.17.3 1.18.4 1.19.5)
# test.sh takes its extra args from build-elixir.yml, which passes "${1%.*}" rather
# than the version. Mirrored here so the local gate matches CI; passing the
# version instead fails an image that runs fine.
boot_args() { echo "${1%.*}"; }
declare -A PKG=(
  [1.17.3]=elixir-1.17
  [1.18.4]=elixir-1.18
  [1.19.5]=elixir-1.19
)
render() { echo "s|__PKG__|${PKG[$1]}|g"; }
