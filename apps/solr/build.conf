# STATUS: BLOCKED (do not publish)
#   Trivy 0-CVE gate on solr:10.0.0 = 47 fixable CVEs (4 CRITICAL / 21 HIGH).
#   PRIMARY blocker (Jul-2026 jackson cluster): jackson-databind-2.20.0.jar
#     (STANDALONE, server/solr-webapp/webapp/WEB-INF/lib/) — CVE-2026-54512,
#     54513, 54514, 54515, 54516, 54517, 54518. Fixed only in 2.18.8 / 2.21.4 /
#     3.1.4 — there is NO 2.20.x patch, so the same-major-PATCH swap the gate
#     allows cannot clear it (2.20 -> 2.21 is a minor bump; 3.x is a major).
#   Strategy (a) exhausted: 10.0.0 is the NEWEST upstream Solr release
#     (archive.apache.org tops out at 10.0.0 / 9.10.1) — no release bundles the
#     Jul-2026 fixes, so a newest-only trim changes nothing.
#   SHADED blocker: CVE-2025-55163 (netty-codec-http2 MadeYouReset HTTP/2 DoS)
#     is shaded inside io.grpc:grpc-netty-shaded-1.65.1.jar — the inner netty
#     cannot be swapped standalone (fix = grpc-netty-shaded 1.75.0, a minor bump).
#   Further core libs need MINOR (not patch) bumps with no same-major-patch fix:
#     opentelemetry-api 1.56.0 -> 1.62.0 (CVE-2026-45292); netty core
#     (netty-handler / netty-transport-native-epoll 4.2.6 -> 4.2.13/4.2.15).
#   Re-evaluate when Solr ships a release that refreshes jackson to >=2.21.4.
#
# solr: from-source via melange (installs the official arch-independent Apache
# Solr binary distribution on openjdk-21-jre). Latest 3 stable minor lines, newest
# patch each (9.9, 9.10, 10.0; all run on Java 21). Each pins its tarball by sha512.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(9.9.0 9.10.1 10.0.0)
declare -A SHA512=(
  [9.9.0]=7b93dab3f0fd09c820a45574c4ef60dff0e8245b8b3a8913bc5874b6e12595ebbd3bb9c856a213ba1643673e461041e95e7e85031523dfb208686c41c366825d
  [9.10.1]=23915ba0c9eba81d9ed7dd46bf3dfa748a1cf12cfd1d2bc3c37e3022893b8d45a6d6dc078ee79e33c0367191c3e4f2d1df3c6f5705331ccfe13d6b1287812eb0
  [10.0.0]=825ab4e92a839802d9f5d570600b622881ddfe4f700cab569afb7d8220590a59d11088f4e10d4dcf9395073c35d73a420cc275a9ed7b18e2f84178bfcb49ae82
)
render() { echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g"; }
