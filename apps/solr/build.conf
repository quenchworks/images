# STATUS: UNBLOCKED 2026-09-20. The zlib block is gone: Wolfi shipped the fix
# for CVE-2026-85091 as zlib 1.3.2.1_rc20260601-r0 and the advisory was amended
# to name that version, so the 1.3.3-r0 this file used to wait for is a number
# that will never arrive. A stock wolfi-base scan reports the advisory as
# `zlib 1.3.2-r7 -> 1.3.2.1_rc20260601-r0`, which is what Wolfi publishes on
# both arches today. scripts/blocked.py now watches the shipped version.
#
# Evidence, stated exactly as measured:
#   x86_64  gated locally 2026-09-20, PUSH=0: 0 fixable CVEs AND the boot test
#           passed, so this is "runs", not just "scans clean".
#   aarch64 not measured locally; this host kills background melange builds and
#           a qemu-emulated arm build does not fit the foreground timeout. CI's
#           native arm64 runner is the first arm measurement, and its gate
#           refuses to publish on any finding.
# STATUS: shippable (0-CVE gate passes on 10.0.0; UNBLOCKED 2026-07-09)
#   Previously BLOCKED with 47 fixable CVEs -- but EVERY finding was a STANDALONE jar
#   (no fork-required shaded blocker). melange.yaml now does a SHA256-pinned standalone
#   jar swap for the 10.0.0 line (guarded), bumping each family in lockstep:
#     jackson 2.20.0 -> 2.21.5   (Jul-2026 CVE-2026-54512..54518; databind needs 2.21.5)
#     jetty   12.0.27 -> 12.0.36 (jetty-http/-server smuggling/DoS; whole server set; Jul-2026)
#     netty   4.2.6 -> 4.2.16.Final (codec-http/http2/handler smuggling/DoS cluster; Jul-2026)
#     calcite 1.37.0 -> 1.42.0 core+linq4j (sql module; CVE-2026-46718 unsafe reflection)
#     log4j   2.25.3 -> 2.25.4  (CVE-2026-34479 log4j-1.2-api bridge DoS)
#     opentelemetry 1.56.0 -> 1.62.0 (CVE-2026-45292; incl. -alpha incubator/prometheus)
#     grpc-netty-shaded 1.65.1 -> 1.75.0 (CVE-2025-55163; bundles the fixed netty)
#     opennlp 2.5.6 -> 2.5.9, zookeeper 3.9.4 -> 3.9.5
#   The optional modules/cross-dc plugin is dropped: it bundles lz4-java 1.8.0 whose
#   only fix (1.8.1) is a POM-only Maven release with NO jar, so it is unswappable;
#   cross-dc (Kafka-based cross-DC mirror) is niche and removing it clears lz4-java +
#   kafka-clients. 0-CVE gate passes and the HTTP API smoke test (boot + core create +
#   index + query roundtrip) passes on 10.0.0.
#   Newest-only: 9.9.0/9.10.1 bundle different (older) jar versions with their own
#   fixable CVEs and the 10.0.0-guarded swap does not touch them, so they are dropped
#   from VERSIONS; re-add only with their own per-version swap sets.
#
# solr: from-source via melange (installs the official arch-independent Apache
# Solr binary distribution on openjdk-21-jre). Newest release only (10.0.0, Java 21).
# Each pins its tarball by sha512.
BLOCKED=0
FROM_SOURCE=1
VERSIONS=(10.0.0)
declare -A SHA512=(
  [9.9.0]=7b93dab3f0fd09c820a45574c4ef60dff0e8245b8b3a8913bc5874b6e12595ebbd3bb9c856a213ba1643673e461041e95e7e85031523dfb208686c41c366825d
  [9.10.1]=23915ba0c9eba81d9ed7dd46bf3dfa748a1cf12cfd1d2bc3c37e3022893b8d45a6d6dc078ee79e33c0367191c3e4f2d1df3c6f5705331ccfe13d6b1287812eb0
  [10.0.0]=825ab4e92a839802d9f5d570600b622881ddfe4f700cab569afb7d8220590a59d11088f4e10d4dcf9395073c35d73a420cc275a9ed7b18e2f84178bfcb49ae82
)
render() { echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g"; }
