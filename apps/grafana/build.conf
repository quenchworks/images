# STATUS 2026-09-20 (second measurement, on 13.2.2): STILL BLOCKED, but the
# blocker is now THREE advisories, not four, and the datasource objection that
# has kept this on the 13.1 line is GONE. Measured by downloading
# grafana-13.2.2.linux-amd64.tar.gz, checking sha256 against dl.grafana.com's
# published .sha256 companion, and reading `go version -m` out of bin/grafana.
#
# What 13.2.2 FIXES:
#   github.com/openfga/openfga  v1.18.0 -> v1.18.1   CVE-2026-61709 closed
#
# What 13.2.2 does NOT fix, still compiled into the prebuilt binary:
#   github.com/apache/thrift  v0.23.1-0.20260429145742  CVE-2026-43871 HIGH
#   otel exporters otlplog/otlploggrpc  v0.19.0        CVE-2026-81871 MEDIUM
#   otel exporters otlp/otlptrace + sdk v1.44.0        CVE-2026-81870 LOW
#
# So a bump alone still cannot reach fixable=0, and VERSIONS stays at 13.1.6.
#
# THE DATASOURCE OBJECTION IS SOLVED, and the note below is now wrong about it.
# 13.2.2 does externalise the datasource backends into gpx_* binaries, but it
# SHIPS them in the tarball under data/plugins-bundled/, prometheus, loki and
# tempo included (14 dirs: elasticsearch, influxdb, jaeger, loki, mssql, mysql,
# opentsdb, postgresql, prometheus, pyroscope, stackdriver, tempo, zipkin). The
# recipe simply never installs that directory. So moving to the 13.2 line is an
# install-path change, not a wait on upstream re-bundling anything.
#
# RE-CHECK CONDITION, narrowed: a grafana release whose bin/grafana carries
# thrift >= 0.24.0 AND otel otlplog/* >= 0.21.0 AND otel otlptrace/sdk >= 1.45.0.
# Read it the way this measurement did, from `go version -m` on the downloaded
# tarball, before spending a build. When that lands, the move is 13.2.x plus
# installing data/plugins-bundled/ and re-pointing the datasource guard at it.
#
# STATUS 2026-09-20: STILL BLOCKED, but the recorded reason below is WRONG and
# points at the wrong fix. Measured today, x86_64, PUSH=0, on the version this
# file actually declares (13.1.6, not the 13.2.2 the note argues about).
#
# 13.1.6 PASSES the datasource guard. prometheus, loki and tempo are all present
# as core in-binary plugins, so the structural objection to 13.2.x does not apply
# to the declared version at all.
#
# What it fails is the CVE gate, with 7 rows over 4 distinct advisories:
#   github.com/apache/thrift  v0.23.1-0.20260429145742  CVE-2026-43871 HIGH -> 0.24.0
#   github.com/openfga/openfga             v1.18.0      CVE-2026-61709 MED  -> 1.18.1
#   otel exporters otlplog/otlploggrpc     v0.19.0      CVE-2026-81871      -> 0.21.0
#   otel exporters otlp/otlptrace          v1.44.0      CVE-2026-81870 LOW  -> 1.45.0
#
# None of these can be floated here. This recipe does NOT build grafana: it
# fetches upstream's prebuilt OSS tarball from dl.grafana.com and installs the
# binary as shipped (see melange.yaml, there is no `go build`). The modules are
# compiled into that binary, so the usual go-module floor has nothing to act on.
#
# So the wait is on GRAFANA to ship a release built against fixed modules, not on
# upstream re-bundling datasources and not on a thrift release (0.24.0 exists
# already and changes nothing while we take a prebuilt binary). The only path
# under our control is building grafana from source, Go backend plus the Node
# frontend, which is real work and not a bump.
#
# Also note the declared version has never published: VERSIONS says 13.1.6 while
# GHCR's newest is 13.1.4. The lock lists 13.1.4 and five older lines because
# gen-catalog falls back to published tags when the declared one is absent.
# RESOLVED 2026-08-27: UNBLOCKED at 13.1.4 with a VEX for the two tempo findings.
#   Gate: "0 fixable CVEs" on x86_64 AND aarch64 (both arches genuinely scanned -- the
#   local gate used to scan only the native arch, fixed in a558599).
#
#   Why 13.1.4 and not newer: 13.2.0 externalised every core datasource backend into
#   prebuilt gpx_* binaries this recipe does not install, so a 13.2.0 image gates green
#   while serving 5 datasource types instead of 17 -- no prometheus, loki or tempo. That
#   would silently break the four shipped stack charts. melange.yaml now carries a guard
#   that fails the build if a tarball externalises a datasource the stacks provision.
#   Verified on 13.1.4: /api/plugins?type=datasource returns 17 types including
#   prometheus, loki and tempo, and each datasource's /health endpoint returns a real
#   backend connection error rather than "plugin not found".
#
#   The residual 2 HIGH are github.com/grafana/tempo and are covered by
#   vex.openvex.json, the first provable false-positives in this catalog. Both CVEs are
#   in the Tempo SERVER (a DoS in its query path; an S3 key leak via its /status
#   endpoint). Grafana is a Tempo CLIENT: reading the built binary, the only tempo
#   packages present are pkg/tempopb and its common/v1, resource/v1 and trace/v1
#   subpackages, and explicit searches return 0 matches for tempodb, modules/, pkg/api,
#   cmd/tempo, pkg/traceql, backend/s3 and pkg/util/status. It is also unfixable by
#   construction: every fixed Tempo release keeps `module github.com/grafana/tempo`
#   with no /vN suffix, so `go list -m github.com/grafana/tempo@v2.10.3` is refused.
#   See docs/vex-policy.md; scripts/check-vex.py validates the file in the gate.
#
#   THIS WAS A LIVE EXPOSURE, not housekeeping: grafana is `status: blocked` in
#   catalog.yaml, which delists it and empties its lock entry, so it was not scanned --
#   while four SHIPPED stack charts pinned grafana 13.1.0 carrying 1 CRITICAL + 17 HIGH.
#   Publishing 13.1.4 and re-pinning those stacks is what closes it.
#
# STATUS: BLOCKED (do not publish) -- re-verified 2026-08-27. Recipe moved 13.2.0 ->
# 13.1.4, which is a large real improvement, but it still cannot pass the gate.
#
# WHAT THE PREVIOUS NOTES CLAIMED, AND WHAT MEASUREMENT SHOWED
#  * 2026-07-09..07-22 claimed the blocker was tempo + openfga v1.14.2 + go1.26.4.
#    PARTLY STALE: openfga and the toolchain were fixed by upstream's own patch line.
#    The tempo half was, and still is, CORRECT.
#  * 2026-08-27 (earlier) bumped the recipe to 13.2.0 and correctly refused its green
#    gate as HOLLOW. CONFIRMED independently: 13.2.0's public/app/plugins/datasource
#    contains only alertmanager, azuremonitor, cloudwatch, dashboard, grafana, graphite,
#    mixed -- prometheus, loki, tempo, mysql, mssql, influxdb, opentsdb, postgresql,
#    pyroscope, jaeger and stackdriver all moved to prebuilt data/plugins-bundled
#    gpx_* backends. 13.2.0 is 0-CVE only because the datasource code left the binary.
#  * A note elsewhere claimed the shipped image has NO tempo findings. THAT IS WRONG --
#    see the gate result below. Every grafana that ships the Tempo datasource in-binary
#    is flagged, including the currently shipped digest.
#
# WHAT IS TRUE TODAY (2026-08-27), every line measured on the actual artifacts
#  * Newest OSS releases AS OF 2026-08-27: 13.2.0, and 13.1.4 as newest 13.1.x.
#    SUPERSEDED 2026-09-14: 13.1.5 has since shipped and is now pinned (13.1.6 404s,
#    so 13.1.5 is the newest of the line). Re-verified that it is still FUNCTIONALLY
#    COMPLETE -- it keeps the datasource backends in-binary, which is the property
#    that rules out jumping to a newer line.
#  * 13.1.4 needs NO module float; upstream already did all of it. `go version -m` on
#    the upstream tarball binary (556 deps) reports go1.26.6 (clears the 9 stdlib rows
#    the shipped 13.1.0 carries), getkin/kin-openapi v0.144.0 (was the wanted fix),
#    openfga v1.18.0 (was the wanted fix), x/net v0.57.0, x/text v0.40.0, x/crypto
#    v0.54.0, x/sys v0.47.0, grpc v1.82.1, otel v1.44.0 -- all at or above the house
#    floor in docs/go-module-float.md. Nothing in this recipe to float: grafana ships
#    upstream's prebuilt binary, there is no compile step to float anything into.
#  * FUNCTIONALLY COMPLETE, verified LIVE on the built 13.1.4 image, not inferred.
#    GET /api/plugins?type=datasource returned 17 types: alertmanager, cloudwatch,
#    grafana-azure-monitor-datasource, grafana-postgresql-datasource,
#    grafana-pyroscope-datasource, grafana-testdata-datasource, graphite, influxdb,
#    jaeger, loki, mssql, mysql, opentsdb, parca, prometheus, stackdriver, tempo.
#    (13.2.0 returned 5.) The BACKENDS really are in-binary, not just frontend
#    metadata: creating prometheus/loki/tempo datasources and calling
#    /api/datasources/uid/<uid>/health returned real client errors from each backend
#    ("Post http://.../api/v1/query: connection refused", "Unable to connect with
#    Loki", "Get http://.../api/echo: connection refused"), never "plugin not found".
#  * 13.1.x externalises exactly TWO backends: elasticsearch (prebuilt on go1.26.3)
#    and zipkin (go1.26.4), both below the go1.26.6 stdlib floor, so neither can be
#    shipped and neither is. This is NOT a regression: the currently shipped 13.1.0
#    tarball externalises exactly the same two, so the live image already lacks them,
#    and no quench chart provisions either type. Recorded in melange.yaml's
#    ACCEPTED_EXTERNALISED so it can never drift silently.
#
# THE GATE RESULT -- WHY THIS IS STILL BLOCKED
# scripts/build-image.sh grafana with ARCHES=x86_64,aarch64 (trivy comprehensive,
# CHML, --ignore-unfixed, no VEX file). Both arches, byte-identical residue:
#   wolfi base 0; all 13 node-pkg datasource targets 0; usr/share/grafana/bin/grafana
#   gobinary: Total: 2 (LOW: 0, MEDIUM: 0, HIGH: 2, CRITICAL: 0)
#     github.com/grafana/tempo v1.5.1-0.20260427112133-525d1bab07e0
#       CVE-2026-21728 HIGH  Tempo: DoS via large queries          fixed 2.8.4/2.9.2/2.10.2
#       CVE-2026-28377 HIGH  Tempo: S3 encryption key disclosure   fixed 2.10.3
# Down from the shipped 13.1.0 digest's 32 findings (1 CRITICAL, 17 HIGH, 13 MEDIUM,
# 1 LOW) -- but 2 fixable HIGH is still a gate failure, so BLOCKED=1 stands.
#
# WHY THE TEMPO PAIR IS NOT FLOATABLE (proven 2026-08-27, not assumed)
# tempo declares `module github.com/grafana/tempo` with NO /vN suffix at v2.8.4,
# v2.10.2, v2.10.3 and v3.0.3 (fetched each go.mod and read line 1). Because a go.mod
# IS present, Go refuses the +incompatible escape hatch:
#   $ go list -m github.com/grafana/tempo@v2.10.3
#   invalid version: module contains a go.mod file, so module path must match major
#   version ("github.com/grafana/tempo/v2")
# and the same for v2.8.4 and v2.10.3+incompatible. So no CVE-fixed tempo release is
# go-gettable under the path grafana requires. Grafana pins tempo by commit, which
# renders as the pseudo-version v1.5.1-0.<date>-<sha>; Trivy ranks that below 2.8.4,
# permanently. This is STRUCTURAL, not staleness: no grafana release that ships the
# Tempo datasource backend can pass this gate, in any packaging shape -- 13.2.0's own
# prebuilt gpx_tempo backend carries the identical 2 HIGH. Building grafana from
# source would not help either; the module path is the same.
#
# THE ONE REMAINING OPTION: a per-app OpenVEX file (NOT added here -- needs a human
# decision, and this agent was instructed not to add one). The evidence for
# not_affected / vulnerable_code_not_present is strong and was collected:
# grafana 13.1.4's binary references ONLY these tempo packages --
#   pkg/tempopb, pkg/tempopb/common/v1, pkg/tempopb/resource/v1, pkg/tempopb/trace/v1
# i.e. generated protobuf message types for the OTLP trace model, and nothing else.
# There is no tempodb/, no modules/, no pkg/api, no cmd/tempo, no S3/object-store
# backend and no query-frontend in the binary. CVE-2026-21728 is a DoS in Tempo's
# query path and CVE-2026-28377 discloses an S3 key via Tempo's /status endpoint --
# grafana runs neither. If that VEX is authored and passes scripts/check-vex.py, this
# app flips to BLOCKED=0 immediately with no recipe change.
#
# ALTERNATIVE REAL FIX: tempo publishes its module under github.com/grafana/tempo/v2
# (or /v3) so the fixed releases become reachable and grafana repins to one.
#
# GUARD -- READ BEFORE ANY VERSION BUMP
# melange.yaml enforces both halves of "0-CVE AND not hollow": a POSITIVE check that
# every datasource the stacks provision still has public/app/plugins/datasource/<id>,
# and a NEGATIVE check that the only externalised gpx_* backends are the two accepted
# above. 13.2.0 fails the positive check by design. Do NOT relax either check to make
# a bump pass, and never trust a green gate alone -- re-run the /api/plugins check.
#
# TRAP THAT COST A FALSE PASS TODAY: apps/grafana/packages/ is a local apk repo and
# apko resolves the HIGHEST version in it. A stale quench-grafana-13.2.0-r0.apk from
# the previous version made the 13.1.4 build report "0 fixable CVEs" while actually
# scanning the 13.2.0 hollow image. Always `rm -rf apps/grafana/packages` after a
# version change, and check the log's "installing quench-grafana (<ver>)" line.
#
# STATUS 2026-09-14: RE-BLOCKED. BLOCKED=0 -> 1. Nightly scan (published digest
#   3ccc567..., still the 08-27 13.1.4 build -- catalog.lock.yaml `published:
#   2026-08-27T08:56:41Z`, never rebuilt since) reports 6 HIGH, matching what a
#   fresh local build of 13.1.4 also reproduces -- this is NOT stale-image drift,
#   it is real: four NEW HIGHs disclosed against grafana's vendored deps after
#   the 08-27 pin, on top of the two tempo HIGHs the existing VEX already covers:
#     CVE-2026-43871  apache/thrift    v0.23.1-pseudo  fixed 0.24.0
#     CVE-2026-56854  x/crypto         v0.54.0         fixed 0.55.0
#     CVE-2026-84304  google.golang.org/grpc v1.82.1   fixed 1.83.1
#     CVE-2026-84445  google.golang.org/grpc v1.82.1   fixed 1.82.2/1.83.2
#   REAL FIX APPLIED: bumped 13.1.4 -> 13.1.5. dl.grafana.com confirms 13.1.5
#   exists (13.1.6 404s -- newest of the 13.1.x line) and 13.2.1 exists (newest
#   13.2.x, still hollow per the 08-27 note's architecture change -- not used).
#   Verified on the downloaded amd64 tarball, not assumed:
#     - public/app/plugins/datasource/ still ships all REQUIRED_CORE_DS the
#       melange.yaml guard checks (prometheus, loki, tempo, jaeger, mysql, mssql,
#       influxdb, graphite, postgresql, testdata, alertmanager) -- NOT hollow.
#     - data/plugins-bundled/ still externalises only {elasticsearch, zipkin},
#       exactly ACCEPTED_EXTERNALISED -- guard's negative check still holds.
#     - `go version -m bin/grafana` on 13.1.5: golang.org/x/crypto is now v0.55.0
#       (was v0.54.0) -- CVE-2026-56854 (fixed 0.55.0) is CLEARED by this bump.
#   WHAT THE BUMP DOES NOT FIX, checked on the ACTUAL 13.1.5 binary, not inferred:
#   thrift stays v0.23.1-0.20260429145742-d2acd3c49e58 and grpc stays v1.82.1,
#   byte-identical to 13.1.4. Also checked 13.2.1 (newest overall OSS release,
#   hollow or not) -- SAME thrift and grpc pins. Grafana's own vendored deps have
#   not moved past 08-27 in either release line, on either side of the hollow
#   split, so no upstream release (checked newest of BOTH 13.1.x and 13.2.x)
#   currently exists that clears thrift/grpc. This is a prebuilt official binary
#   (no compile step here to float thrift or grpc into) -- STRUCTURALLY the same
#   situation as the tempo pair: BLOCKED until grafana upstream bumps thrift/grpc,
#   or a human authors a VEX for CVE-2026-43871/84304/84445 (not done here --
#   same "needs a human decision" rule as the existing tempo VEX; this agent was
#   told never to add one). CVE-2026-56855/78662/GO-2026-5932 (x/crypto, MEDIUM/
#   UNKNOWN) are also present in 13.1.5 (need x/crypto >= 0.56.0, upstream hasn't
#   gone past 0.55.0 yet) but do not change the BLOCKED verdict, which is already
#   forced by the three HIGHs above.
#   NET: this bump is worth taking regardless (real CVE cleared, still fully
#   functional, matches the newest available OSS point release) even though it
#   does not clear the gate -- BLOCKED=1 is honest, not a regression to hide.
#
# STATUS 2026-09-16: STILL BLOCKED, but the block SHRANK from 3 HIGH to 1 HIGH.
#   Bumped 13.1.5 -> 13.1.6 (released since the 09-14 note, which recorded 13.1.6
#   as 404; re-probed today: 13.1.6 is 200 and 13.1.7 404s, so it is the newest of
#   the 13.1.x line. 13.2.2 is the newest overall; 13.2.3/13.3.0/14.0.0 all 404).
#   Every line below measured on tarballs downloaded today, nothing inferred.
#
#   WHAT 13.1.6 CLEARS -- `go version -m bin/grafana`, 13.1.5 -> 13.1.6:
#     google.golang.org/grpc  v1.82.1 -> v1.83.2
#       => CVE-2026-84304 (fixed 1.83.1)        CLEARED
#       => CVE-2026-84445 (fixed 1.82.2/1.83.2) CLEARED
#     golang.org/x/crypto     v0.55.0 -> v0.56.0
#       => CVE-2026-56855 / CVE-2026-78662 / GO-2026-5932 (need >= 0.56.0) CLEARED
#     golang.org/x/net v0.57.0 -> v0.58.0, golang.org/x/text v0.40.0 -> v0.41.0,
#     toolchain still go1.26.6. So the 09-14 note's grpc half is now STALE; only
#     its thrift half survives measurement.
#
#   WHAT REMAINS -- trivy 0.73.0, comprehensive, --ignore-unfixed, CHML, on the
#   13.1.6 gobinary: Total 3 HIGH, of which 2 are the tempo pair already covered
#   by vex.openvex.json. Net after VEX: exactly ONE fixable finding.
#     github.com/apache/thrift v0.23.1-0.20260429145742-d2acd3c49e58
#       CVE-2026-43871 HIGH  TCompactProtocol varint byte-count infinite loop
#                            (CWE-835)  fixed 0.24.0
#
#   WHY NO VEX FOR THRIFT (checked, not assumed -- and the answer is NO).
#   Unlike the tempo pair, the vulnerable code is demonstrably PRESENT. The
#   advisory is in TCompactProtocol's varint reader; `strings` over the built
#   13.1.6 binary returns 246 TCompactProtocol matches including the exact
#   vulnerable surface -- (*TCompactProtocol).readByteDirect, .bytesToUint64,
#   .ReadBinary, .ReadBool, .ReadByte, .ReadDouble, (*TCompactProtocolFactory)
#   .GetProtocol -- all under github.com/apache/thrift/lib/go/thrift. Grafana
#   links thrift for the Jaeger datasource path, so the code is both present and
#   fed remote input. A not_affected/vulnerable_code_not_present claim here would
#   be FALSE. Do not write one.
#
#   WHY IT IS NOT FLOATABLE: thrift v0.24.0 exists on proxy.golang.org (released
#   2026-07-05, tag 6d2ec95), but grafana pins a 2026-04-29 commit which renders
#   as the pseudo-version v0.23.1-0.<date>-<sha>, permanently below 0.24.0. This
#   recipe ships upstream's PREBUILT official binary -- there is no compile step
#   to float a module into (same structural reason as tempo). 13.2.2 carries the
#   IDENTICAL thrift pseudo-version, so neither release line helps.
#
#   THE BLOCK IS NOW TIME-LIMITED, NOT STRUCTURAL -- this is the new fact.
#   grafana's main-branch go.mod already reads `github.com/apache/thrift v0.24.0
#   // indirect`, i.e. upstream has ALREADY merged the fix; it has just not
#   landed in a published tarball yet. Unlike tempo (unreachable by module path,
#   permanent), thrift clears itself on the first grafana release cut after that
#   bump. RE-TEST ON EVERY NEW GRAFANA RELEASE: the moment `go version -m
#   bin/grafana` shows thrift >= v0.24.0, this app goes to BLOCKED=0 and, with
#   the existing tempo VEX, should gate at 0 fixable with no recipe change.
#
#   The 13.1.6 bump is taken regardless: it clears two real HIGHs plus three
#   x/crypto findings, and it is still FUNCTIONALLY COMPLETE -- verified on the
#   downloaded tarball, both melange guards re-run by hand against it:
#     - POSITIVE: all 11 REQUIRED_CORE_DS still ship
#       public/app/plugins/datasource/<id>/plugin.json (20 core datasources
#       total, incl. prometheus, loki, tempo, jaeger) -- NOT hollow.
#     - NEGATIVE: data/plugins-bundled gpx_* backends are exactly
#       {elasticsearch, zipkin} == ACCEPTED_EXTERNALISED -- no new loss.
#   Both sha256 below were computed from tarballs downloaded here and
#   independently matched against dl.grafana.com's .sha256 companions.
#
# grafana: from-source via melange (fetch the per-arch OSS tarball, sha256).
# Newest stable OSS minor line only (full X.Y.Z, used as-is). Per-arch sha256.
# __VER__ = version  __SHA_AMD__ = amd64 sha256  __SHA_ARM__ = arm64 sha256
# MEASURED 2026-09-17: 13.2.2 FAILS THE GUARD. Run 35211194963, both arches:
#   "datasource 'prometheus' is no longer a CORE in-binary plugin in grafana
#    13.2.2 (no public/app/plugins/datasource/prometheus)."
#
# This corrects the blocker on file. The recorded reason was thrift
# CVE-2026-43871, waiting on a thrift release; apache/thrift has since tagged
# v0.24.0, so that wait is over and it changed nothing. The real ceiling is
# structural: 13.2.x still externalises prometheus into a prebuilt gpx_* backend
# this recipe does not install, which is what 13.2.0 was rejected for. A hollow
# grafana would break the four stack charts that provision prometheus, loki and
# tempo, so the guard stops it before the CVE gate is ever reached.
#
# No thrift release can clear this. 13.1.6 is the last of the 13.1 line, so the
# 13 line is frozen here until either upstream re-bundles the core backends or
# this recipe learns to build the gpx_* backends from source and swap them in.
# That second option is the only live path and it is real work, not a bump.
#
# Both sha256 below were computed from tarballs downloaded here and matched
# against dl.grafana.com's .sha256 companions.
BLOCKED=1
FROM_SOURCE=1
VERSIONS=(13.1.6)
declare -A SHA_AMD=(
  [13.1.6]=c6fadb0a4efe53f6a360c962a5c8f8d962be05b1c4657b667c66d5dc69c8215a
)
declare -A SHA_ARM=(
  [13.1.6]=367fbae8d5dcf9b8bb61c0934ab94659cda2d855ebaa6e8c429613e4970d89b6
)
render() { echo "s/__VER__/$1/g; s|__SHA_AMD__|${SHA_AMD[$1]}|g; s|__SHA_ARM__|${SHA_ARM[$1]}|g"; }
