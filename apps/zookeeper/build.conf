# STATUS: BLOCKED (do not publish)
#   Trivy 0-CVE gate cannot reach 0 for zookeeper 3.9.5 (newest release). Two
#   standalone lib/ jars are fixable ONLY across a MAJOR version bump, which is
#   out of policy, and no newer ZooKeeper release ships the fixed majors (3.9.x
#   AdminServer is pinned to Jetty 9.4 and its CLI to JLine 3):
#     - lib/jetty-http-9.4.58.v20250814.jar
#         CVE-2026-2332 (HIGH, HTTP request smuggling) -> fixed 12.0.33 / 12.1.7
#         CVE-2024-6763 (MEDIUM, URI authority parsing) -> fixed 12.0.12
#         => Jetty 9.4 -> 12.x MAJOR bump; 9.4 line is EOL/unpatched.
#     - lib/jline-3.25.1.jar
#         GHSA-2r2c-cx56-8933 (HIGH, Telnet NAWS DoS)
#         GHSA-47qp-hqvx-6r3f (HIGH, Telnet NEW-ENVIRON memory exhaustion)
#         => JLine 3.x -> 4.2.1 MAJOR bump; no 3.x fix exists.
#   The Jul-2026 jackson-databind cluster (CVE-2026-54512..54515) and the netty
#   (CVE-2026-42583/44249/45416/50010/45536) + logback (CVE-2025-11226,
#   CVE-2026-1225, CVE-2026-9828) findings are all STANDALONE and same-major
#   fixable, but they do not clear the gate on their own. Unblock only when
#   upstream ships a ZooKeeper release bundling Jetty 12.x + JLine 4.x.
#
# zookeeper: from-source via melange (installs the Apache ZooKeeper bin dist on a
# JRE). Latest 3 patches of the 3.9 line. Each pins its tarball by sha256.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(3.9.3 3.9.4 3.9.5)
declare -A SHA256=(
  [3.9.3]=19aa2811d5a80c7ae36208ccd61091b5c77812e784c29ffac68b3792ab648f1c
  [3.9.4]=c5b7cbda0b1b59d724e552d33775983f8a366bf651ba484937d0559d97bf0ae9
  [3.9.5]=e752fe205abd9323eef8011ec3cb68adbb83678cfb5ab337159d0a40e12f93d7
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
