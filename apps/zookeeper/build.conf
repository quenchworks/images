# STATUS 2026-09-16: every FIXABLE finding cleared; zlib-blocked only. The CVE-pin block
#   in melange.yaml was version-gated to `if version = 3.9.5`, so the 3.9.6 bump
#   (197eb81) silently shipped the raw upstream dist -- logback 1.3.15 and the whole
#   Jetty 9.4.58 stack came back. The gate is now unconditional and re-verified against
#   the jar list 3.9.6 ACTUALLY ships:
#     - lib/jetty-*.jar + javax.servlet-api -> DROPPED. 9.4.58.v20250814 is the NEWEST
#         9.4.x on Maven Central (the 9.4 line is EOL); CVE-2026-2332 / -10050 / -6790
#         are Jetty 12 only, so there is nothing to swap to. The HTTP AdminServer is off
#         by default (admin.enableServer=false); ZK serves clients over its binary
#         protocol + 4lw commands. TRADE-OFF: no HTTP AdminServer, no Jetty-backed
#         Prometheus exporter.
#     - logback 1.3.15 -> 1.5.38 (SWAPPED, sha256-pinned).
#   Upstream caught up on three old pins -- jline 3.30.14, jackson 2.22.2 and netty
#   4.1.137.Final now ship natively, so those swap lines are GONE. Do not resurrect
#   them from git history: against 3.9.6 they would DOWNGRADE.
#   Measured both arches locally (PUSH=0 ARCHES=x86_64 then aarch64): Total 1 on each,
#   zlib CVE-2026-85091 alone. Unfixable -- the advisory wants zlib 1.3.3-r0 and
#   upstream madler/zlib has not tagged 1.3.3 (newest tag is v1.3.2), so no Wolfi
#   package can exist yet. Flip BLOCKED=0 and rebuild once Wolfi ships 1.3.3-r0.
#
# zookeeper: from-source via melange (installs the Apache ZooKeeper bin dist on a
# JRE). Latest 3 patches of the 3.9 line. Each pins its tarball by sha256.
# UNBLOCKED 2026-09-20. The 2026-09-16 measurement above stands: zlib
# CVE-2026-85091 was the only finding. What changed is the advisory, not this
# recipe. Wolfi shipped the fix as zlib 1.3.2.1_rc20260601-r0 and the advisory
# was amended to name it, so the 1.3.3-r0 the register waited for never arrives.
# Re-gated locally 2026-09-20 at PUSH=0 on x86_64: 0 fixable CVEs AND the boot
# test passed. aarch64 is left to CI's native runner, which refuses to publish
# on any finding.
BLOCKED=0
FROM_SOURCE=1
VERSIONS=(3.9.6)
declare -A SHA256=(
  [3.9.3]=19aa2811d5a80c7ae36208ccd61091b5c77812e784c29ffac68b3792ab648f1c
  [3.9.4]=c5b7cbda0b1b59d724e552d33775983f8a366bf651ba484937d0559d97bf0ae9
  [3.9.6]=6034c9b7b92e60b28ac1f36639f704af98f6d09e74b081664534d3004621d176
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
