# UNBLOCKED 2026-07-09: the two MAJOR-bump blockers were neutralized by DROPPING
#   their OPTIONAL components in melange.yaml (3.9.5 CVE-pin block), then the
#   remaining standalone findings were swapped same-major. Gate now passes with
#   PUSH=0 ARCHES=x86_64 (0 fixable CVEs) and test.sh + a zkCli create/get/delete
#   roundtrip both pass.
#     - lib/jetty-*.jar (Jetty 9.4 AdminServer; CVE-2026-2332 HIGH, CVE-2024-6763)
#         fix only in Jetty 12 MAJOR -> DROPPED. The embedded HTTP AdminServer is
#         disabled by default (admin.enableServer=false); core ZK serves clients
#         over its binary protocol + 4lw commands. javax.servlet-api dropped with it.
#     - lib/jline-3.25.1.jar (JLine 3 telnet; GHSA-2r2c-cx56-8933, -47qp-hqvx-6r3f)
#         fix only in JLine 4 MAJOR -> DROPPED. JLine only added line-editing to the
#         interactive zkCli shell; ZooKeeperMain falls back to a plain reader, so
#         zkCli (create/get/ls/delete) still works (verified).
#   TRADE-OFF: no built-in HTTP AdminServer endpoint and no Jetty-backed Prometheus
#   metrics HTTP exporter (use 4lw commands / an external exporter); zkCli loses
#   arrow-key history but stays functional.
#   Same-major swaps that also had to clear to reach 0: jackson 2.15.2->2.21.5,
#   logback 1.3.15->1.5.35, netty 4.1.130->4.1.135.Final (see melange.yaml).
#
# zookeeper: from-source via melange (installs the Apache ZooKeeper bin dist on a
# JRE). Latest 3 patches of the 3.9 line. Each pins its tarball by sha256.
FROM_SOURCE=1
VERSIONS=(3.9.5)
declare -A SHA256=(
  [3.9.3]=19aa2811d5a80c7ae36208ccd61091b5c77812e784c29ffac68b3792ab648f1c
  [3.9.4]=c5b7cbda0b1b59d724e552d33775983f8a366bf651ba484937d0559d97bf0ae9
  [3.9.5]=e752fe205abd9323eef8011ec3cb68adbb83678cfb5ab337159d0a40e12f93d7
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
