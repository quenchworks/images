# re-gated 2026-07-09 (fresh Trivy DB): STILL BLOCKED, but now by a SINGLE unfixable CVE.
# 8 of the 9 flagged CVE classes are CLEARED image-side via pnpm overrides injected into
# pnpm-workspace.yaml (see melange.yaml): babel-traverse@6->@babel/traverse@7 (rename alias),
# jsonwebtoken 8->9.0.3, nodemailer 8->9.0.1, validator (7.x + 13.12 copies)->13.15.35,
# uuid (3.x + 9.x copies)->11.1.1, multer 2.1.1->2.2.0, vite 7.3.2->7.3.5, esbuild->0.28.1.
# A full local build/scan confirms all of those are gone.
#
# STATUS: BLOCKED (do not publish) — SOLE remaining blocker:
#   @tryghost/members-csv@2.0.7 / CVE-2024-34448 (HIGH, "CSV Injection during member CSV
#   export"). UNFIXABLE via override: Trivy's "Fixed version" 5.82.0 is an internal Ghost-
#   MONOREPO version that was NEVER published to the npm registry — the standalone
#   `@tryghost/members-csv` package tops out at 2.0.7 (exactly what Ghost ships — re-checked
#   2026-07-10 on 6.52.0: core/package.json still pins members-csv 2.0.7, npm latest is still 2.0.7).
#   `pnpm override @tryghost/members-csv 5.82.0` => ERR_PNPM_NO_MATCHING_VERSION. The fix
#   lives only inside a newer Ghost core, so it needs a Ghost upstream release that either
#   republishes members-csv or inlines the fix — not an image-side change. Re-gate when
#   @tryghost/members-csv publishes a >=fixed version to npm, then drop BLOCKED=1 (the other
#   8 overrides already make the rest of the tree 0-CVE).
#
# ghost: from the official npm distribution. Ghost ships as the npm package `ghost`
# (exactly what ghost-cli installs) — an immutable registry tarball with a published
# sha256. The recipe fetches that tarball, installs PRODUCTION deps in a melange `runs`
# step (network is available at build time; native modules like sqlite3/bcrypt compile
# via node-gyp), then runs `node index.js` on Wolfi nodejs-22.
#
# LATEST-ONLY (not the usual latest-3): Ghost is a churny app whose toolchain is pinned
# per release (Ghost 6 requires Node ^22.18.0 via core/package.json engines; older lines
# need other Node majors). Like Excalidraw, no single Node builds every line and a CMS
# has no need to ship stale releases, so the catalog ships the current stable line only.
# Built on Node 22.
FROM_SOURCE=1
VERSIONS=(6.52.1)
declare -A SHA256=(
  [6.52.1]=d2ed2158e10a830556bc07a91d044c80f744b920e1795f573e757bea7417e9d9
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
