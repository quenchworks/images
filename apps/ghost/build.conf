# STATUS: BLOCKED (do not publish)
# 0-CVE Trivy gate cannot pass: 20 fixable node-pkg CVEs in Ghost's PRODUCTION dep
# tree (verified with `pnpm install --prod` — devDeps ARE excluded, these are real
# runtime transitive/direct deps). The dominant ones all need a MAJOR bump (rule =
# SAME-MAJOR only) and Ghost's NEWEST release 6.51.0 still pins the vulnerable
# versions, so a newest-only trim does not help:
#   - babel-traverse@6.26.0 / CVE-2023-45133 (CRITICAL): fix = @babel/traverse@7.23.2,
#     a package RENAME — no `babel-traverse` override can satisfy it. Chain:
#     ghost -> @tryghost/nodemailer@2.3.1 -> nodemailer-mailgun-transport@2.1.5 ->
#     consolidate@0.15.1 -> babel-core@6.26.3 -> babel-traverse@6.26.0.
#   - jsonwebtoken@8.5.1 + express-jwt@8.5.1 / CVE-2022-23539/23540/23541 (HIGH):
#     fix = jsonwebtoken 9.x (MAJOR); Ghost's direct auth deps, express-jwt 8 bundles
#     jwt 8. 6.51.0 still pins 8.5.1.
#   - nodemailer@8.0.11 / GHSA-p6gq-j5cr-w38f (HIGH): fix = 9.0.1 (MAJOR). 6.51.0
#     still pins 8.0.11.
#   - validator@7.2.0 / CVE-2025-12758 (HIGH), CVE-2021-3765, CVE-2025-56200: fix =
#     13.x (MAJOR), via @tryghost/validator@0.2.22.
#   - knex@0.20.15 / CVE-2016-20018 (HIGH): fix = 2.4.0 (MAJOR), via brute-knex@4.0.1.
#   - uuid@3.4.0 / CVE-2026-41907 (MED): fix = 11.x (MAJOR), via request -> node-loggly-bulk.
# Same-major fixes exist (multer 2.1.1->2.2.0, validator 13.12->13.15, vite 7.3.2->7.3.5)
# but cannot clear the gate while the MAJOR-only CVEs above remain. Clearing them needs
# a Ghost major/upstream change (forkable), not an image-side override. DO NOT PUBLISH.
#
# ghost: from the official npm distribution. Ghost ships as the npm package `ghost`
# (exactly what ghost-cli installs) — an immutable registry tarball with a published
# sha256. The recipe fetches that tarball, installs PRODUCTION deps in a melange `runs`
# step (network is available at build time; native modules like sqlite3/bcrypt compile
# via node-gyp), then runs `node index.js` on Wolfi nodejs-22.
#
# LATEST-ONLY (not the usual latest-3): Ghost is a churny app whose toolchain is pinned
# per release (Ghost 6 requires Node ^22.18.0 via core/package.json engines; older lines
# need other Node majors). Like Excalidraw, no single Node builds every line and a CMS
# has no need to ship stale releases, so the catalog ships the current stable line only.
# Built on Node 22.
FROM_SOURCE=1
VERSIONS=(6.49.0)
declare -A SHA256=(
  [6.49.0]=a56d228058a49058af54bde4ffc8f394f806502b12af9196a11f4a9a77de22df
)
render() { echo "s/__VER__/$1/g; s|__SHA256__|${SHA256[$1]}|g"; }
