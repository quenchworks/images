# STATUS: UNBLOCKED 2026-09-19. The zlib block is gone: Wolfi shipped the fix
# for CVE-2026-85091 as zlib 1.3.2.1_rc20260601-r0 and the advisory was amended
# to name that version, so the 1.3.3-r0 this file used to wait for is a number
# that will never arrive. A stock wolfi-base scan reports the advisory as
# `zlib 1.3.2-r7 -> 1.3.2.1_rc20260601-r0`, which is what Wolfi publishes on
# both arches today. scripts/blocked.py now watches the shipped version.
#
# Re-gated locally on 2026-09-19, PUSH=0, BOTH arches: 0 fixable CVEs on each,
# with zlib 1.3.2.1_rc20260601-r0 installed and no CVE-2026-85091 row.
# STATUS: SHIPPABLE (0-CVE gate PASS; unblocked 2026-07-09 via melange jar remediation)
#   Verified 2026-07-09 on pulsar 4.2.3, x86_64: Trivy comprehensive 0-CVE gate PASSES
#   and test.sh boots standalone + round-trips a produce/consume message. Remediation
#   done in melange.yaml (see the "0-CVE jar remediation" step):
#     - STANDALONE same-major swaps (ABI-safe, from Maven Central):
#         lib jackson-databind/core/annotations 2.18.8 -> 2.18.9  (CVE-2026-54515)
#         lib opentelemetry-* core set 1.56.0 -> 1.62.0           (CVE-2026-45292)
#           (whole OTel API/SDK/exporter set bumped in lockstep so it stays consistent)
#     - DROPPED opt/pulsar/instances/java-instance.jar: this functions-runtime uber-jar
#       SHADED jackson-databind 2.18.8 (CVE-2026-54515) and cannot take a standalone
#       swap. It is only exec'd by the Pulsar Functions Java runtime, which this image
#       does NOT run (`bin/pulsar standalone --no-functions-worker`). Core broker +
#       BookKeeper + metadata store are unaffected; the smoke test proves messaging works.
#       TRADE-OFF: enabling the in-image functions worker (PULSAR_FUNCTIONS_WORKER=1)
#       would need java-instance.jar re-added (and would re-introduce the shaded CVE).
#
# pulsar: from-source via melange (installs the official arch-independent Apache
# Pulsar bin distribution on openjdk-21-jre). Latest 3 stable minor lines, newest
# patch each (4.0/4.1/4.2; all run on Java 21). Each pins its tarball by sha512.
BLOCKED=0
FROM_SOURCE=1
VERSIONS=(4.2.4)
declare -A SHA512=(
  [4.2.4]=8c0a63cc6421c8eb3c4a55b8895ca08a38a1c6771758251fc7a82edbc4c6a42bb8ba5c956066b637ec7e049149d467c034e854b571a4cb000f42a47ae6105971
)
render() { echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g"; }
