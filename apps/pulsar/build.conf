#   re-gated 4.2.3 on 2026-07-07: still BLOCKED (same upstream-gated CVE class).
# STATUS: BLOCKED (do not publish)
#   Jul-2026 jackson-databind cluster fails the 0-CVE gate on the NEWEST release
#   (pulsar 4.2.2 — already the latest on archive.apache.org; no 4.2.3/4.1.x/4.0.x
#   ships a fixed jackson, so newest-only trim does not help).
#
#   BLOCKER (SHADED — cannot be fixed by a standalone jar swap):
#     jar : opt/pulsar/instances/java-instance.jar  (14M functions-runtime uber-jar)
#     pkg : com.fasterxml.jackson.core:jackson-databind 2.18.6  (SHADED — 812 classes
#           + META-INF/maven/.../jackson-databind/pom.properties version=2.18.6 are
#           physically embedded inside the fat jar)
#     CVEs: CVE-2026-54512 (HIGH), CVE-2026-54513 (HIGH), CVE-2026-54514 (MEDIUM),
#           CVE-2026-54515 (MEDIUM)   fixed in 2.18.8 / 2.18.9 (same-major patch)
#     why : the fix lives inside an upstream-built uber-jar; only a newer pulsar
#           release that rebuilds java-instance.jar against fixed jackson can clear
#           it, and none exists yet. Same-major standalone swaps in the melange step
#           reach lib/com.fasterxml.jackson.core-jackson-databind-2.18.6.jar but NOT
#           the copy shaded inside java-instance.jar.
#
#   SECONDARY (also unshippable without upstream changes):
#     org.jline:jline-remote-telnet 3.21.0 (lib/org.jline-jline-3.21.0.jar):
#       GHSA-2r2c-cx56-8933 (HIGH), GHSA-47qp-hqvx-6r3f (HIGH) — fixed only in
#       4.2.1 == MAJOR bump (3.x -> 4.x), out of same-major scope.
#     io.opentelemetry:opentelemetry-api 1.56.0 (standalone, fixable) —
#       CVE-2026-45292 (MEDIUM), fixed 1.62.0; a same-major swap would clear this
#       one, but the shaded jackson + jline-major blockers keep the gate red.
#
# pulsar: from-source via melange (installs the official arch-independent Apache
# Pulsar bin distribution on openjdk-21-jre). Latest 3 stable minor lines, newest
# patch each (4.0/4.1/4.2; all run on Java 21). Each pins its tarball by sha512.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(4.0.11 4.1.3 4.2.3)
declare -A SHA512=(
  [4.0.11]=e47b8efb0af0a584a72b79d5af167d074df204e1a47086269f383efbbc6766b79dc0c2f39f651ca28cc492d67f965d06a145ddfc90de642c1e383b44bdfae15e
  [4.1.3]=b088389270296abcfbcb6107823ae780ff9f1b8561ea97c46c51af4520585dd1445e4751e5f9de32b4b7728a0a9a556fcf1dbddc5baf2f1761493e477fd30abe
  [4.2.3]=18805f38e0e8cbd8870bdccb6832505b2c2d42b541d877074aaeacd6ed563b7f06bd1392fda639db52bf2a4ca0f9e6e2b0ddf65026875e337342998c56b7b61a
)
render() { echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g"; }
