# STATUS: BLOCKED (do not publish)
#   Re-triaged 2026-07-08 against pulsar 4.2.3 (newest stable; 5.0.0-M1 is a
#   pre-release milestone, not shippable). GOOD NEWS: upstream 4.2.3 bumped the
#   shaded jackson 2.18.6 -> 2.18.8 (clearing CVE-2026-54512/54513 HIGH + -54514)
#   and the jline-remote-telnet HIGH pair no longer flags. But the gate is STILL
#   RED on ONE remaining shaded MEDIUM:
#
#   BLOCKER (SHADED — cannot be fixed by a standalone jar swap):
#     jar : opt/pulsar/instances/java-instance.jar  (functions-runtime uber-jar)
#     pkg : com.fasterxml.jackson.core:jackson-databind 2.18.8  (SHADED — classes +
#           META-INF/maven/.../jackson-databind/pom.properties embedded in the jar)
#     CVE : CVE-2026-54515 (MEDIUM)  fixed in jackson-databind 2.18.9 (same-major)
#     why : the fix lives inside an upstream-built uber-jar; only a newer pulsar
#           release that rebuilds java-instance.jar against jackson >=2.18.9 can
#           clear it, and none exists yet. The recipe copies lib/ + instances/
#           wholesale (no jar-swap step), and a standalone swap of
#           lib/com.fasterxml.jackson.core-jackson-databind-2.18.8.jar would NOT
#           reach the copy shaded inside java-instance.jar, so the gate stays red.
#
#   SECONDARY (standalone, fixable, but moot while the shaded jackson blocks):
#     io.opentelemetry:opentelemetry-api 1.56.0 (lib/io.opentelemetry-opentelemetry-api-1.56.0.jar):
#       CVE-2026-45292 (MEDIUM), fixed 1.62.0 — a standalone swap would clear this
#       one, but does not clear the shaded jackson MEDIUM above.
#   Unblock when a pulsar release rebuilds java-instance.jar against jackson >=2.18.9.
#
# pulsar: from-source via melange (installs the official arch-independent Apache
# Pulsar bin distribution on openjdk-21-jre). Latest 3 stable minor lines, newest
# patch each (4.0/4.1/4.2; all run on Java 21). Each pins its tarball by sha512.
BLOCKED=1  # held/delisted — see STATUS header above; pipeline skips build+publish
FROM_SOURCE=1
VERSIONS=(4.0.11 4.1.3 4.2.3)
declare -A SHA512=(
  [4.0.11]=e47b8efb0af0a584a72b79d5af167d074df204e1a47086269f383efbbc6766b79dc0c2f39f651ca28cc492d67f965d06a145ddfc90de642c1e383b44bdfae15e
  [4.1.3]=b088389270296abcfbcb6107823ae780ff9f1b8561ea97c46c51af4520585dd1445e4751e5f9de32b4b7728a0a9a556fcf1dbddc5baf2f1761493e477fd30abe
  [4.2.3]=18805f38e0e8cbd8870bdccb6832505b2c2d42b541d877074aaeacd6ed563b7f06bd1392fda639db52bf2a4ca0f9e6e2b0ddf65026875e337342998c56b7b61a
)
render() { echo "s/__VER__/$1/g; s|__SHA512__|${SHA512[$1]}|g"; }
