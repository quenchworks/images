#!/usr/bin/env python3
# static: minimal static base image, versioned by build DATESTAMP (e.g. 20260621),
# not an upstream release. Nothing to check against — rebuild on a cadence.
print("static               datestamped base image — no upstream version to track")
